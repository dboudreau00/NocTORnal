# 00. Decisions and open questions

## Decisions taken in this scaffold

| # | Decision | Because | Cost if reversed later |
|---|---|---|---|
| 1 | Identity and Person are separate node types | Attribution is an assessment, not an observation | High, every edge repoints |
| 2 | Assertion layer under all graph elements | Retraction, provenance, "what did we know when" | Very high, full rewrite |
| 3 | Postgres as system of record, graph DB optional downstream | Data is relational; analytics want memory-resident anyway | Low, projection layer absorbs it |
| 4 | Bitemporal from day one | Temporal replay and disclosure both need it | Very high |
| 5 | Signed edges | Trust networks are meaningless without negative ties | Medium |
| 6 | Ontology in tables, not enums | Types will change monthly in the first year | Low |
| 7 | Machines propose, humans dispose | Auto-ingested graphs are landfill | Medium |
| 8 | Zanzibar-style authz (OpenFGA/SpiceDB), **SUPERSEDED** | Access is relationship-shaped. **Superseded 2026-07-26, recorded here 2026-09-09:** no engine was adopted. The shipped gate is the five-part check in `security/access.py`, answered from `iam.*` (decision 29); the one relationship the model has (assigned to the case that owns it) is a single join, which does not earn an engine. OpenFGA sat in the compose file for six weeks, called by nothing, and was removed with NATS (R13). Reopen here if relationships ever nest | Medium, and never incurred, because nothing was wired to the engine |
| 9 | Collectors in an isolated zone, no DB access, **NOT REALISED (superseded by the single-process topology, recorded 2026-09-09)** | Contains a burnt-persona compromise. **What is true instead:** collectors run inside the API process; `PersonaVault` (decision 53) lives in the same process as `DATABASE_URL` and the TOTP KEK, credentials are envelope-encrypted at rest and decrypted only inside `use()`, and a compromised API host is a compromised vault. Invariant 7 was reworded on 2026-09-09 to say exactly that. Splitting a collector out (its own process, a queue, no database credentials) is a deliberate not-yet, behind docs/16 L3 (authority to operate a persona at all) and a queue nothing has needed; when it happens this decision becomes true | Medium |
| 10 | Single-tenant | Multi-tenancy on this data class is a liability | High |
| 11 | igraph over NetworkX | NetworkX dies around 50k edges | Low |
| 12 | Legal basis and retention `NOT NULL` | Optional governance fields end up empty | Low |
| 13 | Prosecution-grade evidence, US + Canada (2026-07-24) | Data may support prosecution; WORM store, custody ledger and hash verification are load-bearing Phase 1. Authenticity targets: US FRE 901/902(13), (14) (records of a regular electronic process, hash-value certification); Canada Evidence Act ss. 31.1-31.8 (integrity of the electronic documents system). The canonical, replayable audit hash chain exists for this | Very high, cannot be retrofitted onto evidence already collected |
| 14 | Renamed `lattice` → NocTORnal pre-Phase-0 (2026-07-24) | Folder/product name was decided; the identifier lands in the DB, buckets, key prefix (`noct_sk_`), compose project | N/A, done before anything referenced it |
| 15 | Collapsed duplicate `TOX_ID` selector into `TOX_ID_FULL` (2026-07-24) | Two keys for the same 76-hex observable fragment cross-case pivoting; `TOX_PK` remains the only strong Tox selector (invariant 9) | Low now, high after data lands under both keys |
| 16 | Operating context: law enforcement primary, private CTI secondary (2026-07-24) | Legal-basis vocabulary and disclosure features target LE first (Canada: judicial authorisations/production orders, Stinchcombe disclosure; US: warrants/2703(d), Brady); collection defaults stay conservative. Private-CTI deployments relax configuration, never schema | Medium, vocabulary and export templates rework |
| 17 | Alembic owns the schema; `db/schema.sql` is a generated mirror (2026-07-24) | One authoritative change path (`db/migrations/`, one concern per revision) while keeping a readable end-to-end DDL; initdb loads extensions only (superuser). The hand-maintained reference seed beside it was deleted on 2026-09-15: it had drifted 72 ontology rows behind the generated one, which is what a mirror nothing regenerates always does | Low: squash migrations, regenerate the mirror |
| 18 | Telegram evidence CAPTURE in MVP; automated monitoring adapter deferred (2026-07-24), **PARTLY REVERSED (decision 134)** | Telegram is where current-market crime happens, so analysts must be able to file Telegram captures as first-class evidence (manual/assisted export upload with full custody). The high-risk part (persona-driven automated monitoring (MTProto, FLOOD_WAIT, overt joins)) stays deferred; monitoring can be handwork for now. RSS + XenForo still prove the pipeline. **Partly reversed 2026-09-25, recorded here the same day:** the capture half stands unchanged; automated monitoring is now built, behind the two-person collection authority (decision 69), the egress proxy (decisions 68 and 107) and the AMBER ceiling (decision 103), with every Telegram connection leaving on the persona's own exit and a direct route refused in every environment (decision 134) | Medium, capture-format parsing is reusable by a later adapter, so little is thrown away |
| 20 | Selector hardening pass, Alembic 0018 (2026-07-24) | Adversarial review found wrong-merge/missed-merge normalisers: Telegram IDs now keep chat/user namespaces, JIDs drop the resourcepart, Session IDs case-fold as hex, MXID localparts stay case-sensitive (spec), TLSH loses the T1 prefix, onions/IBANs/asdot-ASNs/phone extensions canonicalise, IDNA2008 replaces stdlib IDNA2003 (faß.de ≠ fass.de). `FORUM_UID` (unscoped, per-venue), `PDB_PATH` and `CODESIGN_CN` (attacker-controlled text) demoted from is_strong. FORUM_UID may return to strong once the norm form is venue-scoped | Low now; every deferred day of a wrong-merge normaliser poisons selectors |
| 151 | The audit log will be under a policy of its own (case members read their case's timeline, review history and triage state), not a read for the system role alone; it stays deferred until its readers move (2026-09-25; apps/api rls_registry.py) | Case members read the timeline, review history and triage state on the request connection, and a system-only read would take all of them out from under row security | Medium, every audit reader |
| 150 | Sample intake, minimisation and ingest scoring are system purposes, and so are the officer's screening record, similarity index administration, a manual run or capture, the document hold and every counting readiness probe (2026-09-25; apps/api db.py `SystemPurpose`) | Each must see rows its requester cannot: a hidden duplicate, every body to minimise, every watch, the whole deployment | Medium |
| 149 | A merge's re-pointed ties are counted at the reader's view; the merge re-points every tie and the audit row keeps the full count (2026-09-25; apps/api merges.py) | The conservative reading: a count of ties above the reader is withheld material, and no withheld notice changes | Low |
| 148 | Case-less reference and governance rows (the global tag taxonomy, the global stoplist, case-less watches, deployment-wide two-person requests) are open to every bound user at the database, and the routes decide (2026-09-25; Alembic 0122 to 0124) | They carry no case material, and the permission catalogue lives in code | Low |
| 147 | A table whose label derives from other rows keeps only the case term in its policy (`collect.proposal`, `core.selector`); its label stays an application predicate over the fact functions (2026-09-25; Alembic 0118, 0123) | The contact-block leg is a reverse reference that only an anti-join or a per-row definer call could express, and a selector upsert must meet the case's existing row | Low |
| 146 | A label computed as the strictest of joined rows reads each leg through `iam.element_facts`, never a LEFT JOIN to a policied row (2026-09-25; apps/api proposals.py, lookups.py, notify_events.py) | Under a policy a hidden leg drops out of the join, and the strictest label silently becomes a lower one: the lookups and the proposal source view failed open this way | Medium |
| 145 | A row that belongs to no case (a collected document, a YARA rule set, a Lab sample's own labels) is held to the reader's case-less ceiling; a sample's case is held to the same reach without an assignment (2026-09-25; Alembic 0118, 0121) | A grant on one case never raises a row that belongs to no case, and the Lab is gated by labels, not by assignment | Medium |
| 144 | A legal hold that changed no row is refused, never audited as placed (2026-09-25; apps/api retention.py) | Under row security a hold on an exhibit above the officer would otherwise change nothing and report success; it now runs as a system purpose | Low |
| 143 | Row security is ENABLED, never FORCED, and a table is either fully under policy with every reader converted or listed as deferred with its work named (2026-09-25; apps/api rls_registry.py, test_rls_registry_pg.py) | A half-enforced table fails open in whichever reader nobody converted; the registry test names any table carrying labels that is in none of the three lists | Low |
| 142 | Work that must see every row is a named system purpose: purges, holds, lock extension, merges, withheld counts, the retirement guard, chain verification, the Lab's proposal, and every script (2026-09-25; apps/api db.py `SystemPurpose`) | One named list of the connections that bypass row security, each auditable, instead of a connection that sometimes does | Medium |
| 141 | The gate reads facts and content reads go through policy: `iam.case_facts` and `iam.element_facts` answer the labels and status of any id, and a case's code only through a relationship (2026-09-25; Alembic 0111, 0117) | The gate must refuse what it cannot see without the refusal reading as absence, and the code is withheld material | Medium |
| 140 | A request's connection is bound by a proof derived from the raw session token, and the session row stores its hash; the sample origin binds a spent ticket's holder for five minutes and never holds the system role's connection string (2026-09-25; Alembic 0110, 0111, apps/api db.py) | A binding the request could choose is a binding an injected statement could choose; the sample origin serves hostile bytes | High |
| 139 | The IAM plane is read-only to the request role, and every IAM write runs on a system connection after the gate; two column grants remain (session activity, ticket redemption), each confined by a guard (2026-09-25; Alembic 0109, 0112) | A request role that could write sessions or tickets could rebind its own connection, so a statement injected into a request would escape every policy | High |
| 138 | Four database roles: the owner (migrations only), `noctornal_app` (requests, subject to policy), `noctornal_worker` (system purposes, bypasses row security, owns nothing) and `noctornal_egress` (the egress proxy, which reads an exempt collection plane) (2026-09-25; Alembic 0108, db/init/10-app-role.sh, scripts/runtime_roles.py) | Work that must see every row needs a role that bypasses row security without owning the tables, so it still cannot switch off the append-only triggers | High |
| 137 | Row-level security mirrors the gate: assignment, clearance with the break-glass raise, compartments, and an element's labels composed over its case's; the verb and the step-up stay in the application (2026-09-25; Alembic 0114 to 0124, apps/api rls_registry.py) | A query that forgets the gate's check, or a statement injected into a request, then sees only what its user may read; policies that decided verbs would need the permission catalogue in SQL | Medium, every policy |
| 136 | How a Telegram chat is read: provenance follows the access mode, which only ever moves from public to member; joins go by username only; no media is collected (2026-09-25; Alembic 0106, apps/api telegram_service.py, telegram.py) | Provenance is the fact interception law turns on (docs/16 L4), so a chat read without joining is OPEN_GROUP and a chat read as a member is PERSONA_PARTY, and a source never moves back from member to public. Invite links stay on a device and are never typed into the product. Media is never downloaded (L1). A service message becomes a sentence carrying its TL class name, never an id. A recheck covers 48 hours and 100 ids, a deletion mark is never cleared, and a first poll reads 200 messages. A chat the reader may not see that duplicates one they add is answered as unresolvable. Outcome sentences avoid the shapes the credential redactor masks | Low: each is a reading rule in the adapter, and the stored rows keep their provenance |
| 135 | A Telegram persona is enrolled on the server by a signed-in operator, never stores its phone number or two-step password, and keeps one egress profile for life, burnt or not (2026-09-25; Alembic 0105, apps/api telegram_service.py, scripts/telegram_persona.py) | The operator signs the script in with a password and a current TOTP code, never a recovery code, and the api_id is registered per persona. Telegram links accounts it has seen from one address, so a burnt persona's exit is the one exit that must never carry the next persona: no two Telegram personas share a profile in any status, and the database refuses a change of profile. A locked persona is enrolled again only through a logout and the enrolment gate | Medium: an exit shared by two personas links them for good, and nothing afterwards can unlink them |
| 134 | Telegram monitoring is built on MTProto with Telethon, behind the collection authority, the egress proxy and the AMBER ceiling, and a direct route is refused in every environment (2026-09-25; Alembic 0105 to 0107, apps/api telegram.py, telegram_service.py, telegram_wire.py, the `telegram` extra). **Partly reverses decision 18 and answers open question 8** | Decision 18 deferred the persona-driven adapter for its risk, not its value. The risks it named are now held by other decisions: every read needs a confirmed two-person authority (decision 69), every connection leaves through the proxy on the persona's own exit and only for a live run, act or stop (decision 107), and the Telegram ceiling is capped at AMBER (decision 103). Telethon is a maintained MTProto client with a SOCKS5 path through python-socks, which is how it reaches the proxy; it and pyaes are single-maintainer code on the host that holds every persona's session (docs/17 F31). Capture of Telegram exports as evidence is unchanged | Medium: switching monitoring off loses no capture path, and the sources, chats and messages already collected stay under their authorities and retention |
| 133 | Forum signatures, quoted post ids, reactions and member profiles are kept in side tables keyed by document version, read only under the document's and source's labels, and deleted by the retention purge; member profiles are FORUM_MEMBER documents fetched only when asked for (2026-09-25; Alembic 0104, apps/api forum_adapters.py) | A signature is one fact about its author, never 4,000 observations; reactions are cheap, informative edges; a quote's source says which post a post answers. All of it is the author's personal data, so it goes with the document. FORUM_MEMBER is a collect-only category outside ingest's vocabulary, and member pages are read only when a source's `member_pages` is above zero | Low: side tables of a document version |
| 132 | One forum at a time: a per-origin lock spans every source on that forum for the whole poll, and an anti-bot challenge is recorded as BLOCKED, never as parser drift (2026-09-25; apps/api forum_adapters.py) | Two sources on one board polled at once would double the request rate the board sees and put two sessions in its log. A challenge is the board refusing the reader, not a page the parser misread, and consecutive challenges or rate limits lengthen the wait up to a day | Low |
| 131 | A forum post is judged deleted only between two survivors read again on contiguous pages, never in a run that drifted, and MyBB times are read only under the board's declared zone and formats (2026-09-25; apps/api forum_adapters.py, forum_parse.py) | A post missing from a page that shifted is not a deletion, and a false deletion is a false fact about an author. A MyBB time is read title first and never taken as midnight; an unreadable time is a note on the run, not drift | Low |
| 130 | A public forum read never leaves from this server's own address: with no egress proxy every forum source is refused before any request (2026-09-25; apps/api forum_adapters.py, readiness row `forum_collection`) | A board's operator reads its access log, and the investigating host's address is the one that must never appear there. `NOCTORNAL_FORUM_ALLOW_DIRECT=1` allows a direct read in development only; a production start refuses it, and the readiness row fails while it is set | Low |
| 129 | Forum pages are parsed in a bounded child process (2026-09-25; apps/api forum_parse.py, forum_adapters.py) | lexbor 0.4.12's tree builder is quadratic on markup a hostile board can serve: measured on 2026-09-24, 4 MiB of unclosed `<a>` took over 25 seconds and 200,000 nested `<div>` took 107. The child limits its own CPU and memory, the parent kills it at a wall clock, and an abandoned page is parser drift. The parsers call selectolax's lexbor backend only | Low: the child is a wrapper around the parse |
| 128 | A sandbox report is a machine SANDBOX analysis read as hostile input, and nothing is proposed from it unless the operator turns that on (2026-09-24; apps/api sandbox.py, `NOCTORNAL_SANDBOX_AUTOPROPOSE`) | CAPE's YARA names are kept in the findings. Auto-propose is off by default and, set to config, proposes payload-configuration values only, through the proposal path (invariant 3). A part of a report the reader does not understand becomes an extraction gap; a report that cannot be fetched is retried and given up after `NOCTORNAL_SANDBOX_GIVE_UP_AFTER_S`; one over the size cap in both forms ends at once | Low |
| 127 | A detonation can be sent to one self-hosted CAPEv2, configured from the environment, as the encrypted archive only, by the sandbox worker alone, over the `sandbox` integration route (2026-09-24; Alembic 0103, apps/api sandbox.py, sandbox_capev2.py, scripts/sandbox_dispatch.py, the `lab-cron` compose service) | A sandbox is integrated, not built (docs/11), and a self-hosted one keeps the sample off a vendor's platform. The target's exposure and ceiling are the operator's declarations with no default, and the CAPE network route and the analysis machine are second exposure dimensions. A send to an exposed target, or on a live route or machine, needs the named authoriser's sign-off in the product within 72 hours; it is listed with the other two-person controls rather than being an approvals operation. Raw submission is never used and nothing is resent; NOT_SENT means the request never left. The SHARED custody row names the requester and is committed before the POST. Preflight requires CAPE's API and web interface to refuse unauthenticated reads. The Lab workers run in a compose loop of their own, apart from the notification drain | Medium: every SUBMIT row is the record of what left, and 0103's downgrade refuses while one exists |
| 126 | The Security Officer's screening match list is label-free, and screening alerts carry no content (2026-09-24; apps/api screening.py, the officer's section under Oversight) | An officer must see every match whatever its labels. The list shows a hash prefix, the time, the list names, the disposition, the alert and review state and the section's counts; the full record opens only within the officer's ceiling and never shows the filename, source note, reason, analyses or custody. Alerts are URGENT, GREEN, case-less and content-free, coalesced to one open alert per recipient per hour; the case owner is told a sample was withdrawn, at the sample's labels. Whether counts across compartments are acceptable there is for the owner (docs/17) | Low |
| 125 | A screening match is permanent and takes the sample out of the Lab for everyone: a held match is always preserved, a matched submission is preserved or, where the deployment destroys rejected material, never stored, and nothing matched is destroyed in the product (2026-09-24; Alembic 0102, apps/api screening.py, samples.py `LAB_EXCLUSIONS`) | Destroying prohibited material can itself be the offence (decision 61). A case hold forces preservation. Destruction outside the product is recorded as DISPOSED_OUTSIDE, on counsel's instruction. The worker never changes a data key: an absence of the bytes is recorded after two looks and reads as needing a review until the officer records one made after it. The retrieval authorisations of a matched sample become void by derivation, and retiring a list never un-matches a sample | High, by design: a match cannot be undone, and 0102's downgrade refuses while one exists |
| 124 | Prohibited-content screening compares exact md5, sha1 and sha256 digests with operator-imported lists held in Postgres; perceptual matching is not built (2026-09-24; Alembic 0102, apps/api screening.py, scripts/sample_screen.py) | A perceptual matcher decodes hostile images and has no wheel for every supported platform. Two authorities are recorded before anything is imported: the ingest policy and `NOCTORNAL_HASH_SET_AUTHORITY`, copied onto each list, and each list carries its own licence reference (docs/16 C3). The Security Officer holds both verbs (`sample.screening.manage`, `sample.screening.review`) and one officer imports; two-person import is the recorded alternative. Screening compares against the lists this deployment imported, so no match does not mean the material is lawful to hold | Low: the lists are data |
| 123 | Outbound lookups: a provider registry whose keys are sealed and bound to their origin and route, an exposure ladder whose lowering takes a second administrator, and a host switch that ships off (2026-09-24, amended 2026-09-25; Alembic 0098 to 0101, apps/api providers.py `ProviderVault`, lookups.py, lookup_adapters.py) | Two acts before anything leaves: an administrator enables a provider, and the host operator sets `NOCTORNAL_OUTBOUND_LOOKUPS=on`. One integration route per provider (`lookup-<key>`). NONE is checked as far as the first hop, through a route entry naming a private network. An approval is bound to the origin and private network it was asked about, and moving a provider below PUBLIC to another host, port or network is a new determination, held by the database. Personal data is refused, and a sample's hash leaves only once the sample is screened with no match against every active list. Quota is counted from the attempt ledger with a queued reserve; a per-case cache is served only at or above the subject's label; answers become Triage proposals, never graph; a withheld answer says nothing about itself. The sign-off rule is decision 75 | High for the sign-off (decision 75); low for the rest, which is configuration |
| 122 | Jira is an opt-in notification channel: one operator-declared destination on the `jira` route, with a sealed credential, a ceiling (GREEN by default, AMBER at most), field exposure and a per-case veto, and nothing inbound (2026-09-24, amended 2026-09-25; Alembic 0097, apps/api jira.py) | A work item copies intelligence into a system with an audience of its own (docs/16 C17). The stricter of the row's ceiling and the environment's applies; routing is an allowlist in code, and case-less notifications are never routed; field exposure defaults to SUBJECT. One issue per work item, idempotent by a reference label, per-event claims and a settle window; a create of unknown outcome is reconciled after it, even when its delivery was withdrawn. On an egress refusal Jira gets nothing, not a stub. Jira's answers are refused unread past 32 levels of nesting, and a Jira failure never stops email, webhooks or the review producers. A Data Center network rides on the declared rule (`NOCTORNAL_JIRA_NETWORK`). Not dual control: step-up plus audit. No inbound webhook, because a receiver is a write primitive into the platform. Answers open question 7 for outbound use | Low: switching the channel off leaves the link records, which the purge warning reads |
| 121 | The delivery ledger records a stable cause and what actually left for every delivery, and it lives in Administration, Integrations (2026-09-24, amended 2026-09-25; Alembic 0095 and 0096, apps/api transports.py `CAUSES`) | Code matches on the cause, never on the human sentence. Exposure (STUB, SUBJECT or SUMMARY) is recorded per delivery, with a backfill derived from the code that ran. A webhook address is withheld behind a fingerprint, because a hook URL often carries a secret in its path. Requeue puts FAILED and backing-off rows back at attempt zero, never inside Jira's two-minute settle. SMTP and webhooks leave by their integration routes; a webhook is https-only in production and follows no redirect; a configuration gap holds a delivery and only an attempted send fails it. `notify_outbox_draining` never blocks, at a 30-minute threshold. An account whose only Admin flag is `integration_manage` opens on Integrations. The webhook signature carries no timestamp until a versioned scheme exists (docs/17 F28) | Low |
| 120 | Similar wording is shown as a band, never a number, and a claim's similarity is told only as its reader may know it (2026-09-24; apps/api embeddings.py, http/routers/similarity.py) | A bare score reads as a probability that one author wrote both, which it is not. The lowest band also needs a shared phrase or selector: unrelated posts from one market scored up to 0.23 with nothing in common. A claim's stored reason is told only when the reader can read everything the claim cites; otherwise what their own readable facts give, or that it cannot be compared | Low |
| 119 | No row lock is held across an audit write or a network send, and a model server's answer is hostile input (2026-09-24; apps/api embeddings.py) | Each batch is two short transactions around the work, and `EMBED_BATCH_SENT` and `EMBED_QUERY_SENT` are committed before anything leaves. An answer that is not a set of finite, non-zero vectors fails its batch with backoff and an audit row, and any other failure after the send fails the batch; nothing wedges the queue or re-sends a batch every pass | Low |
| 118 | Similarity vectors carry their document's labels, set by trigger, and are deleted with any change to its labels, text, category or purge; reads walk per-label partial indexes plus an exact compartmented branch and join back to the live document (2026-09-24; Alembic 0092 and 0093) | A reader's nearest-neighbour search must walk only rows it may read, so rows above it can neither be returned nor crowd out the rows it may. `read_compartments` is bound under decision 71. Exhibits and claims are compared by an exact scan inside one case. The pass drains a queue the triggers fill, rather than walking the corpus every time | Low: vectors are derived and re-embedded |
| 117 | A model endpoint is inside this host only when it is a declared loopback answer on a direct route outside production; every other endpoint, and every endpoint in production, is outside, and victim data is never embedded in any space (2026-09-24; apps/api embedders.py, egress.py `MODEL_HOST`, `MODEL_REMOTE`) | Outside means invariant 8's floor, a declared ceiling, a written authority (`NOCTORNAL_EMBED_MEANING_AUTHORITY`) and the withholding of messages, unvetted and uncleared categories and closed cases; compartmented material goes to no endpoint (docs/16 D12). Victim data (a high-risk category, or a compartment an ingest key forces) is judged again on every pass | Medium: loosening it sends case text to a server this product does not control |
| 116 | Similarity has two roles: similar wording from a built-in, versioned embedder that runs on this host and sends nothing (on by default), and similar meaning from an operator's model server through the `embeddings` route (off by default) (2026-09-24; Alembic 0091 to 0093, apps/api embedders.py, embeddings.py, scripts/embed_pass.py) | Wording finds reposts, light edits and the same passage transliterated without anything leaving the host, which a deployment can accept with no determination; meaning needs one (decision 117). Vectors are compared only inside one space and a rebuild is a new space. Only a role's first index registers itself: after an administrator retires a role's last index, only Rebuild starts another, because a new meaning index sends every eligible text again. `collect.document.embedding`, which nothing ever wrote, is removed | Low: vectors are derived |
| 115 | Web Key Directory lookups are an integration route (`wkd`): off by default, named directories only, and asked for by one person and approved and sent by another (2026-09-24; Alembic 0090, apps/api pgp_keys.py, `NOCTORNAL_WKD_CEILING`) | A lookup tells the directory's operator which address was looked up, when, and from where (docs/16 C15). The record is committed before the first packet, only the hash leaves, no redirect is followed and no User-Agent is sent, a request lapses, and asking, approving and declining each need a recent sign-in. A wildcard or suffix entry keeps lookups off, because the build cannot tell a key provider's directory from one the people under investigation run | Low |
| 114 | Vendor keys are case-scoped records of what was obtained and from where, never born confirmed, and a binding is confirmed only when a cited contact block ties the signing key to the binding's holder (2026-09-24; Alembic 0088 and 0089, apps/api pgp_keys.py, pgp.py) | A guarantor's signed vouch, checked with the guarantor's own key, would otherwise confirm the vendor's binding: the escrow error with a signature on it. Labels run block <= key <= binding. A good signature with no such tie is recorded UNATTRIBUTED, and the verification ledger is never rewritten or deleted | Medium: bindings confirmed before 0089 are listed for verifying again (docs/17) |
| 113 | The PGP verification queue and ledger share one visibility predicate, and writes on the Comms PGP routes are judged against the caller's own labels, never through a case-scoped break-glass grant (2026-09-24; apps/api pgp.py, pgp_keys.py) | A check the reader may not see is reported to them as no check, rather than as a row they cannot open. A break-glass grant is for reading in an emergency; it must never upgrade a binding | Low |
| 112 | gpg never starts an agent here, every key and detached signature is walked for OpenPGP framing before gpg sees it, and a gpg below the floor (2.4.9, 2.5.14 or 2.2.51, and no 2.3) counts as no verifier unless the operator attests a patched build (2026-09-24; apps/api pgp.py, `NOCTORNAL_GPG_PATCHED_AS`) | Builds below the floor carry the armour-parser and status-forgery CVEs, and a distribution build may carry the fixes under an older number, so an attestation is accepted and recorded on every verification row. Secret key material is refused outright. A detached signature is checked beside the file it signs, and a signature by a signing subkey now confirms a claim of its published primary key | Low |
| 111 | Production topology: the application network is internal; caddy and the egress proxy alone reach out; Tor and VPN sidecars sit on `exits`, which no integration entry may name; a local model server sits on `models`, reached only through an `embeddings` entry naming that network (2026-09-24, amended 2026-09-25; infra/production/compose.yml, apps/api egress_routes.py) | An integration entry that could name an exit could leave wearing a persona's identity, and one that could name the deployment's own services could reach the database. The egress keys live in `egress-proxy.env` and `egress-client.env`, and the proxy role's password in `postgres-init.env`, never in `secrets.env` | Medium: the compose networks and `NOCTORNAL_EGRESS_INTERNAL_CIDRS` are held equal by a test |
| 110 | Egress configuration: `egress.manage` (SYS_ADMIN, step-up) for every write, `egress.log.read` (SYS_ADMIN, SECURITY_OFFICER) for the connection log, and a headless script that signs its operator in with a password and a current TOTP code (2026-09-24; Alembic 0085, apps/api egress_admin.py, scripts/egress_setup.py) | Where anything may go is a different question from what an integration sends (`integration.manage`), so it has its own permission. The script never takes a recovery code or an issued password, and `adopt` creates all of its proposal or none of it. The readiness rows and the production start refusal state outbound uses as presence or counts, never names, and never count rows a label hides from some administrator | Low |
| 109 | Every proxy connection is recorded in `collect.egress_connection`, append-only and hash-chained, which only the proxy's own database role writes (2026-09-24; Alembic 0086, apps/api egress_ledger.py `EGRESS_GRANTS`, db/init/20-egress-role.sh) | The record of what left the building must not be writable by the process whose traffic it records. `seq` is drawn inside the chain lock, so concurrent writers cannot fork the chain. Every persona row names what labels it (the matched source, else the run, else the persona's bound sources), and the reader applies that source's current label and compartments; a persona row naming none of them never shows its host. Integration rows are GREEN. "Every connection is logged" is read as: probe connections are not logged and never dial or resolve; refusals before authentication are logged as counts per peer per minute; development writes no row | Medium: the ledger is never purged by this build (docs/16 D11), and 0086's downgrade refuses while it holds a row |
| 108 | An exit is sealed to the egress proxy's own key: the API seals it and cannot open it, and its fingerprint makes a new exit a widening and a password change not (2026-09-24; Alembic 0085, apps/api security/egress_seal.py) | A residential pool or VPN credential is a secret the API has no use for. The fingerprint covers kind, host:port and user name under a separate key that is never rotated. HTTPS is an exit kind of its own. A residential or VPN exit used in clear needs an audited acknowledgement. Chained exits receive names unless the profile opts into resolving at the proxy, so a persona's forum names never reach the deployment's resolver; a local sidecar that is not Tor is handed a checked address, because it would resolve inside this host | Medium: re-sealing every exit |
| 107 | A persona route needs a live run, act or stop, judged from the database, never from the client (2026-09-24, amended 2026-09-25; apps/api egress_authz.py, Alembic 0085) | A run's destination must be its source's site, under an authority recorded and confirmed after the profile last widened and after the persona or source was last bound again; widening a profile or binding again voids older authorities, `reach_changed_at` is set by the database alone, and the binding history is append-only. An act (an enrolment, a resolution, a join) reaches a source's site only through that source's own target, judged as a run's authority is; an address in the profile's networks needs one passing target, and a persona with no source target yet reaches only its profile's own networks, under a live authority of its own, and never a name. The passive default is matched by a feed run's empty profile. A machine lock refuses a run and not the act that enrols a new credential. Stops (a logout) are always allowed, within 60 seconds, 256 KiB and four an hour per persona. Every open persona tunnel is checked again every 30 seconds and closed when its context stops holding | High: the proxy is where collection authority meets the network |
| 106 | A poll records itself: a held source is left out of the schedule rather than recorded as a failure, raw markup is kept per item in its own bucket with no object lock and deleted with its document, and the existing unique key serves the collector's lookups (2026-09-24; Alembic 0081, apps/api collection.py, rawstore.py, `COLLECT_RAW_BUCKET`) | A source held for want of an authority, an exit or a persona is configuration, not parser health, so it never counts toward DEGRADED. Raw markup lets a parser fault be replayed, and it is the author's personal data, so it goes with the document. A second document index would duplicate `(source_id, external_id, version)`. A run's custody log of what it asked for is written once and never rewritten | Low |
| 105 | A persona's machine holds cannot be undone by hand, one usability rule decides whether a persona may be used, and a person reaches a persona only with their own ceiling (2026-09-24; Alembic 0082, apps/api collection.py `PERSONA_USABLE_SQL`, `PersonaVault`) | A platform's wait kept in the column a person sets could be shortened in two steps, so the machine's columns are separate, a guard refuses any shortening, and a lock clears only with a new sealed credential. The lease, the gate, the due list and the egress proxy all ask the same predicate, so they cannot disagree. A missing ceiling is allowed only for the unattended worker and is refused whenever a person is acting. The persona egress check names only personas the caller may see and says one bit about the rest | Low |
| 104 | A collection authority has two scopes, PUBLIC_READ and MEMBER_READ, and no active scope; it is per persona or persona-less, and each source under it is a target snapshotted at its address and binding and confirmed by a second person (2026-09-24; Alembic 0083, apps/api collection_authority.py) | Nothing in the product posts, messages or buys, so an active scope would authorise nothing the product does. A target covers its source only while the source's address and the exit it is read through are what the confirmation saw. The confirmer's role never records an authority and never runs the collection it covers. A run records who pressed Poll now as a plain id with no foreign key, so the run ledger never refuses to record a poll over an identity | Medium: authorities and targets are never deleted, and 0083's downgrade refuses while one exists |
| 103 | Collection ceilings are declared per kind (`NOCTORNAL_FORUM_SOURCE_CEILING`, `NOCTORNAL_TELEGRAM_SOURCE_CEILING`), capped at AMBER, and unset means that collection is off (2026-09-24; apps/api config.py, egress.py `Destination.COLLECTION_TARGET`) | Every request an exit makes tells the site what the deployment reads, so the fact of reading crosses the boundary and invariant 8's floor applies. Compartmented and above-ceiling work is collected by hand, through capture | Low |
| 102 | Compiled YARA builds are an insert-only history keyed by engine, platform and the host's CPU features as the host reports them, authenticated by a key derived from the KEK ring (2026-09-24; Alembic 0080, `lab.yara_compiled`) | A compiled build carries native code, so a build that fails its MAC or will not load is recorded as rejected and a newer one is built; a unique index would have made that rebuild impossible. Listings check a build's seal over its recorded digest and never read the blob or write; a build is rejected and rebuilt only on the paths that use it. Rule bundles are parsed off the event loop behind a guard that counts the zip directory it actually reads, so no bound depends on an end record the uploader wrote | Low |
| 101 | YARA rule sets are labelled, versioned records that a Security Officer who did not sponsor them activates, writing down the licence clearance (2026-09-24; Alembic 0080, apps/api yara_rules.py, Lab, Rules) | Third-party rules carry licences somebody has to clear, and the product only records the clearance (docs/16 C14). yara-x is an optional extra. A rule file compiles in two passes, and a file with any error contributes no rule. A set's labels only rise, and its compartments change only through the registry's rename. A set's key is unique only among sets carrying the same labels, so creating one never confirms a set above the creator's. Retrohunt is manual, scoped to the caller's view, names the caller in custody, and is refused for an inactive version | Low: 0080's downgrade refuses while any rule set exists |
| 100 | Static triage is a queue drained outside the upload, and each step runs in a self-limiting child process fed over stdin, which never decrypts (2026-09-24; Alembic 0079, apps/api lab_triage.py, lab_static.py, scripts/lab_triage.py, the `lab-triage` compose service) | Parsing hostile bytes has produced CPU and memory blow-ups, so it cannot run inside a request. The parent decrypts and verifies, and validates or looks up every value and reason a child reports; a child is bounded by rlimits on Linux and by a wall clock everywhere. The queue is drained by its own compose loop and by an on-demand request that answers 202 and starts after the response when a deployment-wide slot is free, so there is no resident worker (decision 30 holds). An analyst's request is claimed first, then submissions and retries, then retrohunts and backfills. Liveness is proved by session advisory locks, never by a clock. Triage of samples held before this release is the operator's audited command (`--backfill`), never a migration, because it decrypts every sample | Medium: the child is the only boundary between a parser exploit and the process holding the deployment's secrets (docs/16 L1) |
| 99 | ssdeep and TLSH are ported into the tree and held to reference vectors (libfuzzy 2.14.1, py-tlsh 4.12.1) rather than taken from a package (2026-09-24; apps/api fuzzyhash.py) | The pure-Python ssdeep package scores differently from libfuzzy, so its scores would not compare with anybody else's, and C bindings have no wheel for every supported platform. NOTICE.md carries both upstream notices | Low |
| 98 | Machine findings never write the graph and nothing is proposed automatically: an analyst proposes a machine selector, with origin `lab/static-triage` (2026-09-24; apps/api samples.py `PROPOSAL_ORIGINS`, lab_triage.py) | Invariant 3. A YARA family reaches a case only as an analyst's assessment with a confidence. The prohibited-content screening gap is derived at read time, never stored, so it cannot go stale. Degenerate ssdeep digests are not stored, and the .NET loader-stub imphashes are flagged as common, because both would link unrelated samples | Low |
| 97 | An analysis derived from a YARA rule set is read through the set's labels as well as the sample's, and a reader who cannot see a rule set never meets its identity (2026-09-24; apps/api samples.py, yara_rules.py) | A unit's rules say what it hunts. Without the set's labels such an analysis is hidden and uncounted; custody keeps the person and time and drops its kind and id for that reader; the sample row, the run summary and readiness show counts at most | Low |
| 96 | Custody can name the product itself: a read nobody asked for is recorded with `actor_kind` SYSTEM and no person, for SCANNED, VIEWED_META, REJECTED and ANALYSED only, and a verified read writes one SCANNED row per person whose request caused it (2026-09-24; Alembic 0078, apps/api samples.py `_verified_plaintext`) | Static triage and screening read bytes on nobody's request, and naming a person there is false, while a synthetic account would hold a role and could sign in. A download, a share, a detonation request or an assignment always names a person, which is the question custody exists to answer. Retries carry their requesters, and scheduled work adds one SYSTEM row. The verified read refuses to run inside a transaction, so its tamper alarm always commits | Low: 0078's downgrade refuses once a SYSTEM or SCANNED row exists |
| 95 | Retired `iam.permission.requires_dual_control` and `iam.dual_control_request` (2026-09-24; Alembic 0077) | Nothing read the column and nothing wrote the table, and the column disagreed with the code: case deletion read as two-person while marking a case PURGED took one signature. A second, wrong answer to "which operations need two people" is worse than none. The migration refuses, naming the count, if the table holds any row, because such a row was made by hand | Low: the downgrade restores both exactly |
| 94 | A case's merge switch: turning it on takes one signature, turning it off takes a second holder of `case.update` on the case, bound to the switch's epoch, and under ALWAYS a case cannot turn it off (2026-09-24; Alembic 0076, Triage, Dual control) | Turning a control off is exactly what a borrowed session wants, and the switch took one signature both ways. The epoch moves on every change, so an approval granted against one "on" cannot be banked across an off and on and spent later. Whether that second person needs a seasoning rule like decision 92's is open (open question 12) | Low |
| 93 | Approval requests are write-once where it matters, and each policy approval is bound to the policy it was raised against (2026-09-24; Alembic 0074, apps/api approvals.py, `GET /approvals`) | A trigger that trusts a row anybody could rewrite checks nothing. What a request asks for never changes, its times only move earlier, its state only moves forward, and its decision and consumption are written once at the database's clock; DELETE stays allowed, because deleting a pending request denies rather than forges. One deployment-wide approvals route serves the officer's queue. Counts cover only cases the viewer is assigned to and cleared for. Operators may add separated pairs and remove the ones they added; pairs installed with the software stay. The readiness row `dual_control_policy_changeable` informs and never blocks | Low |
| 92 | The seven-day countersigner rule, both ways: for seven days after someone else resets or re-enrols an account's credentials, reactivates or unlocks it, or gives it a countersigning role, it cannot countersign; nor can a countersigner who did any of that to the proposer (2026-09-24; Alembic 0074 and 0075, `iam.countersign_blocked_by`) | It stops one person with two administrator accounts taking over the officer's half, and the officer who made the proposer's account. It is checked at the countersignature, with a sentence naming who did what and when (UTC), and again at apply against a `decided_at` the database pinned. Refusing is never blocked. Every refusal is audited out of band, but only when the caller's transaction does not hold the audit-chain lock (otherwise on the caller's own connection), and the side connection waits at most 5 seconds for a lock | Low |
| 91 | Two modes, PER_CASE and ALWAYS, and no NEVER: floors live in code and the database can only make an operation stricter (2026-09-24; apps/api approvals.py `OPERATIONS`, `iam.dual_control_operation`) | A missing or unknown row reads as ALWAYS, and only operations with more than one mode (node merge today) are configurable. Operations registered and not yet enforced (case deletion, role changes, persona credential reveal) are shown as "Not enforced yet" with the reason, rather than wired in passing. Going from ALWAYS back to PER_CASE leaves each case's switch as it was. Signature lifetimes stay in code | Low |
| 90 | The two-person policy changes only through two people: an administrator proposes, a Security Officer who holds no administration countersigns, and the administrator applies it as a separate step, in both directions (2026-09-24; Alembic 0075, apps/api dual_control.py, Administration, Two-person controls) | Which operations need two people lived in code and in `iam.separated_duty`, with no screen and no record of who decided. `dual_control.manage` (SYS_ADMIN) and `dual_control.countersign` (SECURITY_OFFICER) are a declared separated pair, and because `iam.separated_duty` separates roles rather than people, an account holding `manage` through any role never countersigns. Loosening and tightening both take two people, and a proposal nobody else could countersign is refused. The policy tables change only by a write that exactly matches a ledger row applied in the same transaction, and the ledger pins `applied_at` to the database clock, so a compromised API process cannot move the policy through a trigger of its own. SECURITY DEFINER was not used; the binding protects a database with no separate runtime role equally | Medium: the ledger is append-only and holds the policy's history, and 0075's downgrade refuses while the policy is stricter than its default |
| 89 | The Analysis pane checks whether the runs on screen still hold with one request, and background checks with venues projected run at most once every 30 seconds (2026-09-24; `GET /cases/{id}/analytics/currency`) | One request per card meant one projection per card. With venues projected, background checks would otherwise spend the one-mode budget an explicit Run needs | Low |
| 88 | Roles by CONCOR, one to four splits, with numpy held to one BLAS thread per process (2026-09-24; apps/api blockmodel.py, `GET /cases/{id}/analytics/concor`, readiness row `role_analysis_thread_capped`) | Roles find entities in the same position whether or not they are tied. The card states the fit first, shows each relation's block densities with the tied blocks marked in words, and lists pairs alike but not tied as leads. A BLAS pool per process would multiply the cores by the workers, so the cap is set through threadpoolctl when it is installed and otherwise through OpenBLAS's own setter, and the readiness row reads it back (Apple's Accelerate cannot be capped). CONCOR has its own run, meter and cache key, and direction is in its key only; positions are kept in `analytics.community_assignment` by run; refusals are known before a run is recorded; rows are kept on rollback. REGE is not built | Low: derived results |
| 87 | "Accepted ties only" is an analysis scope: every measure can be computed over the ties a reviewer accepted, and each tie left out is counted by review state (2026-09-24; apps/api projections.py, the analytics routes' `review_scope` parameter) | Alpha 6 could say how many ties behind a result were unreviewed or disputed, but could not compute without them. It is "accepted" rather than "reviewed" because a disputed tie is a recorded doubt. SUPERSEDED and unknown states are counted too. The scope is hashed into the cache key only when it is on, so every stored name and digest is unchanged | Low |
| 86 | numpy, selectolax and pefile are installed with the API; Telethon and python-socks form the optional `telegram` extra, and yara-x the optional `yara` extra; cryptography 47 or later is required (2026-09-24; apps/api pyproject.toml, constraints.txt, the image's `NOCTORNAL_EXTRAS` build argument) | Each was checked against the wheel bar on 2026-09-24. A dependency that is heavy, or lacks a wheel for a supported platform, is an opt-in extra, and the feature it serves says so on the readiness register when it is absent. ssdeep and TLSH are ported into the tree (decision 99), so no fuzzy-hashing package is needed. CI runs the whole suite with every extra on one leg, and the tests marked `extras_absent` with none on the other. NOTICE.md lists each distribution with its licence | Low: an extra becomes a hard dependency, or the reverse, by editing the pins |
| 85 | The egress gate refuses a destination that has no gate record, and a destination that crosses the boundary keeps invariant 8's floor and refuses compartmented material by construction (2026-09-24; apps/api egress.py `_GATES`, `GateRule`) | Until this build a destination missing from the crossing set was treated as in-app, so a member added without its line failed open. Each destination now has one gate record, `IN_APP` is the only one that returns early, and a `GateRule` that crosses the boundary and drops the floor or allows compartments cannot be built. The collection target, the key directory, the model endpoints, the lookup providers and the sandbox each also require a declared ceiling | Low |
| 84 | `egress_policy.py` is the one classifier of outbound addresses and the one table of egress codes and wire names; the proxy, the pinned client and every integration import it (2026-09-24; apps/api egress_policy.py, docs/20 sections 2 to 4) | Two classifiers are two answers to "may this process reach that address", and the one that drifts is the leak. Private space is reachable only through a rule naming the host and its network, or a network rule an IP literal falls in, on a listed port. Cloud metadata addresses, Azure's 168.63.129.16 (public space) included, and the deployment's own networks are refused on every route. A host with a trailing dot or a non-ASCII name is normalised, and a numeric host name such as `2130706433` or `0x7f.1` is refused | High: every consumer and the proxy decide destinations through it |
| 83 | The `ingest.record(duplicate_of)` index is built inside the migration's transaction; an INVALID index of that name is rebuilt, and a valid one with another definition is refused (2026-09-24; Alembic 0072) | The upgrade stops the API first, so an in-transaction build is safe, and an operator with a very large table may build it CONCURRENTLY beforehand under the same name. A failed concurrent build leaves an INVALID index that `IF NOT EXISTS` would accept and the planner never uses; a different valid index of that name is somebody else's | Low |
| 82 | A seeded retention rationale is reworded only while nobody has touched it (2026-09-24; Alembic 0073) | Two seeded rationales cited design documents an operator does not have. A rule somebody confirmed or edited is theirs, even if it cites a document, so only a rule still exactly as seeded is reworded, with an audit row, and no period changes | Low: the downgrade restores the seeded text where the new text is untouched |
| 81 | `url_norm` keeps a fragment only on hosts whose fragment names the resource, and only in a shape that cannot hold a key or a token; stored selectors are listed, not rewritten (2026-09-24; packages/ontology normalisers.py `url_norm`) | Dropping every fragment collapsed fragment-keyed links (`mega.nz/#!...`) into one selector. Keeping every fragment would put a MEGA decryption key or a Telegram Web login token into a selector, which is stored, indexed and shown. So a fragment is kept for MEGA in its current form without the key, matrix.to without `via`, web.telegram.org chat forms only (never a login token or a bot start parameter), and twitter.com's `#!/` form; any other fragment is dropped, and links in prose lose trailing punctuation | Medium: the normaliser is a correlation key, so a change re-keys selectors |
| 80 | Records written under older rules are listed, never filled or moved: Triage claims with no observation date, ATTRIBUTE claims readable below their material or attached across cases, and captures no compartment lock fits (2026-09-24; scripts/legacy_records.py, readiness rows `triage_claims_dated`, `triage_claims_within_labels`, `captured_documents_compartmented`) | Filling an observation date would rewrite a claim, which invariant 5 forbids, and moving a claim or relabelling an entity needs an analyst's judgement. So they are counted on the readiness register and listed per case. Whether the dates may be filled waits on an owner decision amending invariant 5 (open question 11) | Low: listing changes nothing |
| 79 | A case walled off for victim data still refuses capture (2026-09-24; apps/api extraction.py `CaptureService`) | Victim-data compartments (the compartment any ingest key forces, revoked keys included) hold data about people who are not subjects, whose handling waits on docs/16 L2 and decision 52. A capture there would put third-party material into the collection outside that regime | Low: lifting it is one refusal, once L2 is settled |
| 78 | A capture into a compartmented case is stored under the case's compartments, and every read of a collected document checks them (2026-09-24; Alembic 0070 and 0071, apps/api extraction.py, collection.py document readers, proposals.py, contact_blocks.py, `test_document_reads_check_compartments.py`) | A collected document belonged to no case and had no compartments, so Alpha 6 refused every capture into a compartmented case, leaving those analysts no way to capture at all. A capture now copies its case's compartments and deduplicates only under the identical, unpurged lock; every list, search, watch hit, claim card and Triage read checks `d.compartments <@` the reader's; an accepted NODE or EDGE carries only the material's compartments beyond its case's; an entity's effective compartments include its case's for the ATTRIBUTE rule, and accepting refuses an entity outside the proposal's case; a contact block cites only a document its case already cites. 0071 labelled the captures made before, from the cases that cite them, raising classification to the least citing case's and never lowering it | Medium: 0070's downgrade refuses once a document carries compartments, because dropping the column would declassify it |
| 77 | The outbound client, the route object, the route provider and the proxy wire contract are one fixed contract, documented in docs/20 (2026-09-24; apps/api egress_policy.py, pinned_http.py, egress.py, egress_routes.py, egress_proxy.py) | The collection foundation, the egress proxy and the Telegram adapter all speak it, and three readings of one interface would disagree at the first connection. It fixes: one listener at `NOCTORNAL_EGRESS_PROXY_URL` serving HTTP CONNECT and SOCKS5; the wire username `route[~context]`, with run, act and stop contexts for persona routes; one closed table of refusal codes in egress_policy; `egress_routes.py` as the route provider, found by module name with no registration call; and development applying the same policy directly, with no ledger. Decisions 68 and 72 are read through it | High: every consumer, the proxy and the tests that hold both ends are written to it |
| 76 | Row-level security is written after every other feature, against the complete schema (2026-09-24). **Built 2026-09-25: decisions 137 to 151; 66 tables under policy, 15 still deferred (docs/17 F51)** | Policies written table by table while tables were still arriving would each have needed rewriting, and the tables added since Alpha 6 (collection authorities, forum and Telegram side tables, rule sets and screening records, lookups, the egress ledger) each carry label rules of their own. Until it lands, a SQL injection inside a request still reaches every row the API role can (docs/17, Deferred security items) | None yet; each table added before it lands is one more policy to write |
| 75 | A lookup that sends case material to somebody else's system takes two people: one asks, and a named, eligible colleague signs it off in the product, in a separate action, within 24 hours; there are no standing authorisations (2026-09-24; Alembic 0099, apps/api lookups.py `LookupService.sign_off`, dual_control.py `OTHER_TWO_PERSON_CONTROLS`) | VENDOR and PUBLIC exposure both put a selector into a third party's system, so both need the second person, not PUBLIC alone. The database holds it: a lookup row is born AWAITING_SIGNOFF, QUEUED or CACHED with nothing sent and nothing signed, a sign-off is recorded once by the authoriser the requester named, and a row that is not NONE is sent only while that sign-off stands and before it lapses. A planned batch has no second person, so batches and the unattended drain send NONE lookups only. Web Key Directory lookups (decision 115) and sandbox sends to an exposed target (decision 127) follow the same rule | High: standing VENDOR authorisations would let one person send case material to a vendor with no second look |
| 74 | No code arms the purge of collected documents without honouring a legal hold on the document AND on every case that cites it (2026-09-24; Alembic 0084, apps/api retention.py `RetentionService`, `set_document_legal_hold`) | The collector now writes `collect.document.retain_until`, which arms the document leg of the purge for the first time. A hold reaches a document through every citing case and every version, and an unretracted claim citing it pins it. A document's clock comes only from a confirmed or seeded category rule, and no FORUM_POST or FORUM_MEMBER rule is seeded, so forum documents stay unclocked until an operator confirms one, which readiness shows. A hold on a document carries its reason and who placed it; the reason is read only by holders of `retention.manage`, and everyone else sees that a hold exists. A purge empties a document's text, title, address, author identities and raw markup, and deletes its side rows (forum signatures, reactions and profiles; Telegram message facts), in one transaction | High: a purge that misses a hold destroys material a court ordered kept |
| 73 | One-mode projection for forums and wallets, chosen per run and never a rewritten preset; conversations are not projected this way (2026-09-24; apps/api affiliation.py, the analytics routes' `one_mode` parameters). **Answers decision 33's "still open" for these two families** | Centrality over a two-mode graph answers the wrong question (decision 33). Forums and channels can be projected to co-poster ties, and wallets to co-controller ties and to ties from a payer's controller to a payee's, Newman-weighted so a shared venue counts less the more entities share it. Venue sizes and caps come from every visible membership before the confidence floor and the review scope, so neither shrinks a venue under its cap. Only undoubted identity links (ACCEPTED or PROPOSED) suppress a tie, and a direct PAID tie in either direction suppresses a derived flow. Two memberships tie only if they overlap in time, a membership's time being the union of its dated rows, and a derived tie is dated to the latest period its constituents share. Two limits, each refused by an arithmetic pre-count before any work: 50,000 derived ties, and 1,000,000 steps of comparing dated periods. Derived ties are marked derived, never stored and never drawn. Conversations stay with the Comms pane's co-participation projection, because projecting them here would lose decision 58's incidental-party and room-size protections, and the Communication preset keeps its mode warning | Low: derived, so recomputing is the whole cost |
| 72 | There is one outbound HTTP client: `pinned_http.fetch_response`, with `open_connection` for protocols that are not HTTP, and every outbound request in the product goes through it (2026-09-24; apps/api pinned_http.py, `test_egress_single_exit.py`, docs/20 section 5) | Before this build only the collector's fetch was hardened, and every integration that needed HTTP would otherwise have written its own: a pool that resent a POST after a TLS error, a proxy path that shook hands outside the time limit, a classifier that admitted metadata addresses. The one client connects only to an address it checked (direct) or tunnels to the name through the egress proxy; bounds the whole call by one wall-clock allowance, every hop, the proxy dial and the TLS handshake included; never retries; never lets a credential or a body follow a redirect off its origin; accepts a pre-resolved hop only as it issued it for the same route and mode, and admits its addresses again before dialling; and redacts everything it reports. A test scans the tree for any other socket, client library or hand-built route | High: a second client is a second place for the next SSRF |
| 71 | The triggers are the compartment registry: `iam.compartment_bindings()` reads every bound column from the catalog, `iam.compartment_in_use` asks each and fails closed, and every column storing compartment keys, derived copies included, is bound the same way (2026-09-24; Alembic 0069, docs/05 "Binding a compartment column", `test_compartment_contract_pg.py`) | 0059 rendered the guard as a fixed eighteen-column union in a released migration, so no later migration could add a column without restating the function, and whichever restatement ran last would have dropped every other column from the guard: a key could then be retired while rows were still filed under it. Now a column is bound when its table carries the `compartments_registered` trigger; a later migration declares `ADDED_BOUND_COLUMNS` and adds a line to `compartment_lifecycle.BOUND_COLUMNS` and `NOUNS`, and none restates the three registry functions. A copy deleted whenever its source changes is still a copy, so the similarity vectors' `read_compartments` is bound too | Medium: a column outside the registry is one a rename skips and a retire ignores |
| 70 | The Lab has one pipeline: static triage, prohibited-content screening and the sandbox share one label gate, one exclusions list, one verified read of a sample's bytes and one way of recording a machine analysis, and each later step extends those seams rather than restating them (2026-09-24; apps/api samples.py `lab_gate`, `LAB_EXCLUSIONS`, `_verified_plaintext`, `MACHINE_PRODUCERS`) | Three features read hostile bytes and write findings about them, and each with its own reader would be three places to forget a label check or a screening match. Every Lab reader applies `LAB_EXCLUSIONS` (a screening match is one line in it), the system readers included, and only the Security Officer's content-free match list is exempt; every read of the bytes goes through the one path that verifies them against the recorded SHA-256 and writes custody; every machine finding is a `sample_analysis` row of a producer the list names | Medium: a second reader of sample bytes would have to repeat the verification, the custody row and every exclusion |
| 69 | Every forum and Telegram source needs a live two-person collection authority and a declared classification ceiling before anything is read from it, public boards included; RSS is unchanged, and every adapter builds on one frozen contract (2026-09-24; Alembic 0081 to 0083, apps/api collection.py `run_once`, `refusal_cause`, collection_authority.py, `test_collection_contract.py`) | Nothing recorded authority to collect, and docs/16 L3 described a control (ACTIVE_ENGAGEMENT) that never existed. Every forum and Telegram read is now refused unless a collection authority, recorded by one person and confirmed by another, covers the source, and unless the deployment declares a ceiling for that kind of collection. A public board is included because reading one as a persona, or from an attributable exit, is still an act somebody has to authorise. The adapters build on one shared contract (the cursor round trip, per-poll budgets and pacing, a custody log of what each poll asked for, raw markup storage, the persona lease and its signals, and the refusal, plan and commit hooks), so none redefines them. The authority itself is a document outside the system, and every screen that shows one says so | High: an adapter with a contract of its own, or a read no authority covers, is the unreviewed act docs/16 L3 blocks on |
| 68 | The egress proxy is the only way out of a production deployment, and it carries two kinds of route: persona routes for collection and integration routes for operator-configured services (2026-09-24; apps/api egress.py `route_for`, egress_routes.py, egress_proxy.py, infra/production/compose.yml, docs/20) | Collection through a persona and a delivery to a mail relay need different rules, and before this build nothing enforced either at a network boundary: every process dialled out from the investigating host's own address. In production the API, the cron loops and the sample origin sit on an internal network and the proxy alone reaches out. A persona route (`persona:<egress profile>`) reaches the public internet only: the proxy refuses private, loopback, link-local, CGNAT and metadata space, resolves once and pins, and applies the profile's destination policy. An integration route (`integration:<name>`: the SMTP relay, the webhook, Jira, the CAPEv2 sandbox, each lookup provider, the embeddings model server, Web Key Directories) reaches only the host:port entries an administrator created (step-up, audited), and private space only through an entry that names it. The proxy speaks HTTP CONNECT with Basic credentials and SOCKS5 with RFC 1929 credentials on one listener, so Telethon can use it through python-socks, and every connection is logged append-only. Every client takes its route from one function, `egress.route_for`. In development with no proxy the same policy is applied in process and the connection is made directly, with no ledger, and the readiness row `egress_boundary` says the boundary is not in force; a production process with outbound uses and no proxy refuses to start. Profiles and routes are configured under Administration, Egress, or with `scripts/egress_setup.py` | High: every outbound consumer asks `route_for`, and the production topology, the connection ledger and the collection authority checks all assume one exit |
| 67 | The API requires a claim's grading: basis, reliability, credibility and confidence (owner, 2026-09-23; apps/api http/routers/graph.py `AssertionBody`) | A body that left the grade out was recorded as a direct observation graded F6 and LOW, which reads as an analyst's judgement nobody made (invariant 1). Every body that records a claim now names all four, a missing one is a 422 naming it, and nothing is written. The console's Correct... became a graded form for the same reason. Corrections recorded earlier keep the defaults they were given, and the console labels them by what they are rather than by that basis | Low: restoring a default is a schema change to the request bodies, and the claims already recorded are untouched either way |
| 66 | A CLOSED or ARCHIVED case is read-only for its CONTENT, and its governance still works (owner, 2026-09-23; apps/api http/deps.py `authorize_object`) | The console said a case was closed and the server accepted writes to it anyway. The check sits in the one function every case-scoped route already calls: a verb in `CONTENT_WRITE_PERMISSIONS`, or a route marked `content_write`, answers 409 "Case is read-only" on a CLOSED, ARCHIVED or PURGED case, with a `CASE_READ_ONLY_REFUSED` audit event. Reopening, legal holds, retention, sharing, break-glass and purge stay open, because those are how a closed case is governed. Reopening a CLOSED case is the way to work on it again; an ARCHIVED one is never reopened | Low: the rule is one set of verbs in one place |
| 65 | A password is reset by an ADMINISTRATOR, never by an emailed link (owner, 2026-09-23; Alembic 0066, apps/api iam_admin.py `reset_password`) | There was no reset at all. An emailed link makes whoever reads the mailbox able to take the account, and a reset goes better through a person who knows the colleague. An administrator (`user.manage` with a fresh sign-in) issues a one-time password, shown once; every session the account had is revoked and any lockout cleared; the next sign-in with it mints no session until the person chooses their own (`must_change_password`). The last administrator is reset from the server's shell with `bootstrap.py reset-password`, through the same service | Medium: a self-service path could be added beside it, and would bring back the mailbox this keeps out |
| 64 | Stay on the pinned MinIO build, and PROVE write-once instead of reading the lock configuration (owner, 2026-09-22; apps/api readiness.py `prove_write_once`) | The open-source MinIO upstream is archived and outside security support (docs/17 F24). The research behind the decision found SeaweedFS accepting a COMPLIANCE retention and then letting the locked version be deleted, so a store can report a lock it does not enforce. The register now writes a canary under a one-day COMPLIANCE retention and passes only when a DELETE naming that version id is refused for retention, on the evidence and the preservation buckets. The risk is accepted in writing, bounded by the store never facing the internet. Ceph RGW is the self-hosted exit and a storage-adapter change | Low: the probe is store-neutral, and any replacement must pass it before an exhibit is written there |
| 63 | Victim PII is two people by construction: the Lead investigator reveals, the Security Officer alone authorises (owner, 2026-09-22; Alembic 0062) | `victim_pii.reveal` was held by no role, so the reveal refused everyone, while both roles could authorise (docs/17 F16). `iam.separated_duty` now refuses any grant giving one role both halves, and the upgrade revoked, with an audit event each, the live authorisations a Lead investigator had granted a co-lead | Medium: the revoked authorisations are not restored by a downgrade, because the revocation happened |
| 62 | Break-glass: the Lead investigator and the administrator invoke, the Security Officer alone reviews (owner, 2026-09-22; Alembic 0062) | Confirms the narrowest default migration 0039 guessed (docs/17 F14). `CASE_OWNER` now displays as Lead investigator with its key unchanged, so no check moved; the analyst stays excluded. Invoke and review are a separated duty, so no later grant lets a role review its own emergency | Low: a role grant, held by the separated-duty guard |
| 61 | A REJECTED malware sample is PRESERVED, not destroyed, unless the deployment declares otherwise (owner, 2026-09-22; Alembic 0063, apps/api samples.py) | Where the law requires prohibited material to be kept for a designated authority, destroying it is itself the offence, and destruction was the default (docs/17 F2). Rejected bytes move, still encrypted and with their data key kept, into an object-locked store (`PRESERVE_BUCKET`, default `noctornal-preserved`) under a legal hold; retrieval needs `sample.preserved.authorise` (Security Officer) and `sample.preserved.retrieve` (Lead investigator). `NOCTORNAL_REJECTED_SAMPLE_DISPOSITION=destroy` restores destruction, which a legal hold still refuses. Whether and for whom to preserve stays with counsel (docs/16 L1) | Medium: preserved objects sit under legal hold, so reversing means lifting holds one by one, deliberately |
| 60 | An element's OWN labels gate every comms read, and the caller's clearance is a REQUIRED argument (2026-07-25, Alembic 0037, apps/api comms.py + pgp.py + contact_blocks.py) | `require("comms.read")` calls `authorize_object` with no classification, and `deps.effective_labels` then returns the CASE's labels -- so the gate decided against the case alone. Every `comms` table carries its own `classification`/`compartments`, and `check_writable_labels` caps a writer at their OWN clearance rather than the case's, so a RED-cleared analyst can write a RED row into an AMBER case and nothing in the schema stops a row sitting above its case. An AMBER-cleared assignee then read it back in full -- `raw_text` is the entire forum post. `read.py`, `evidence.py`, `samples.py` and `coparticipation.py` all filter on element labels; the comms paths did not, and the omission was invisible because the case gate passed. The fix filters in SQL on the caller's ceiling, and `CommsService` now RAISES when constructed without a clearance rather than defaulting to one: a default is how the next caller silently becomes maximally privileged, which is how this arrived. Related: `_visible_cases` bounded cross-case counts by assignment and expiry ALONE, omitting the verb, clearance dominance and compartments -- and `assign_user` performs no clearance check while a case's classification can be raised afterwards, so an assignment the five-part gate refuses is reachable (`assign_user_checked` exists because of it). The impersonation query has no label filter of its own, so it returned publisher handles and source URLs out of a case the caller cannot open | Medium -- the predicates are per-query and each one omitted is a leak |
| 59 | A parser cannot be trusted with a status stream it does not control, and a constraint cannot save it (2026-07-25, Alembic 0037, apps/api pgp.py) | The worst defect this project has produced, found by the third adversarial pass and reproduced end to end. gpg percent-escapes `%` and bytes below 0x20 in the attacker-controlled user-ID field and escapes NOTHING at or above 0x80; Python's `str.splitlines()` breaks on U+0085, U+2028 and U+2029. A key whose user ID embedded `<U+0085>[GNUPG:] VALIDSIG <victim fingerprint> ...` made gpg emit a forged status line inside `GOODSIG` -- which it emits BEFORE the real `VALIDSIG` -- and the parser took the first match. Outcome VERIFIED, signing fingerprint the victim's, binding upgraded to CONFIRMED, for a key the attacker did not hold, from the ordinary submission workflow. **0035's claim that its CHECKs made the trap "unrepresentable" was wrong**: `signing_fingerprint` and `claimed_fingerprint` were both written from the same lied-to parse, so they agreed. A constraint defends against the application FORGETTING to check; it cannot defend against the application checking a forged input. The lesson is that trust boundaries are about the INPUT to a check, not the check. Fix: parse bytes and split on `b"\n"` only; refuse more than one VALIDSIG rather than picking the first; decode attacker bytes with `errors=replace` for the record and never for a verdict (which also closed a separate defect where a non-UTF-8 user ID killed the subprocess reader thread on Windows and returned BAD_SIGNATURE for a good signature, and propagated uncaught on POSIX so NO row was recorded). It was invisible on the cp1252 dev host and live on the Linux/UTF-8 deployment target, so the regression test asserts on raw bytes rather than end to end | High -- CONFIRMED bindings granted before the fix must be re-derived |
| 58 | Co-participation is a PROJECTION, never stored edges, and its cap is reported (2026-07-25, apps/api coparticipation.py) | docs/10 calls the co-participation network "often the cleanest social graph you will get" and docs/03 says how to build one honestly: "Bipartite projection to one-mode with Newman weighting (dividing by event size), otherwise a 500-member forum creates a spurious clique." `analytics._mode_warning` had already recorded this as the open item it closes. It stays a projection because `projections.PRESETS` says why in the Communication preset's own text -- "co-participation is deliberately excluded: it is a projection, not an observation" -- and two people in one 40-person channel have NOT been observed talking to each other. Written as stored edges that distinction dies the moment the first person forgets, which is what invariant 4 exists to stop, so the result is marked `is_inferred` and nothing touches `core.edge`. Newman weighting fixes a big room's INFLUENCE (1/(n-1) per pair), but not its COMBINATORIAL cost -- a 5,000-member channel still yields 12.5M near-zero pairs -- so rooms over `max_room_size` are excluded and each is NAMED in `coverage`, because a cap that silently drops data is worse than no cap: the output looks complete. Three restrictive defaults, all reversible and all reported: unresolved handles are not promoted to vertices (`comms.participant` deliberately does not resolve most group members, since that manufactures actors out of a member list -- doing it here manufactures them one step later); incidental participants get no ties (docs/08, docs/16 L4: a third party in a group channel is not a subject); isolates are not returned, being artefacts of the filters that would sit in the denominator of every density and percentile | Low -- it is derived, so recomputing is the whole cost |
| 57 | PGP verification is delegated, and the two attribution traps are CHECK constraints (2026-07-25, Alembic 0035, apps/api pgp.py) | docs/10: "A message signed by a key whose fingerprint appears in the contact block is real cryptographic evidence of control, not a claim." This is the only path in Phase 7 producing CONFIRMED -- the grade docs/10 says may carry weight in automatic identity resolution -- so it is where being wrong is worst. No cryptography is implemented: `gpg` is driven as a subprocess and ONLY its `--status-fd` output is parsed, never the human-readable text, which is localised, reformatted between versions and historically spoofable via crafted user IDs. **Trap 1, the wrong key**: a signature proves control of whatever key signed it, so verifying against "some key in the keyring" and reporting success is evidence about a stranger; VALIDSIG's fingerprint must equal the CLAIMED one. **Trap 2, the replayed message**: everything after `END PGP SIGNATURE` in a clearsigned message is unsigned, so any message a vendor ever published can be reposted with an attacker's Tox ID pasted below it and `value in message` PASSES -- so the comparison is against gpg's OWN `--output` of the verified region, never the supplied text. Both are also CHECKs on `comms.pgp_verification`, deliberately duplicating the application logic, because these are exactly the checks that survive review and then get refactored away. There is NO `TRUSTED` outcome: the web of trust answers "do I trust this key's owner", a different question from "did this key sign this text", and an investigator's keyring trust has no bearing on whether a vendor controls a key. Expired and revoked keys get their own outcomes -- both still prove the key signed, neither confirms alone. `NO_VERIFIER` is distinct from every failure so "nobody checked" and "checked and failed" cannot be confused | Medium -- CONFIRMED bindings already granted would need re-deriving |
| 56 | The contact-block parser REFUSES ambiguity, and role is independent of kind (2026-07-25, Alembic 0035, apps/api contact_blocks.py) | docs/10 calls contact blocks "the highest-value extraction target" and immediately says why naive extraction is dangerous: blocks routinely carry the forum's escrow agent, a guarantor or a partner shop, and "attributing the escrow's Jabber to the vendor is a serious, and easy, error". The obvious implementation regexes every selector-shaped string out of the post, scores well on recall, and produces the landfill docs/09 warns about -- because the difference between the vendor's Jabber and the escrow's is not in the STRING, it is in the LABEL NEXT TO IT. So a labelled line resolves by label; an unlabelled one resolves only on an UNAMBIGUOUS shape. Bare 40-hex does not (SHA-1 is identical), 64-hex does not (Tox pubkey, SHA-256 and OMEMO all match), `local@domain` does not (a JID and an email are the same shape, and calling one the other misfiles the strongest selector in the block). Refusals are kept as UNPARSED WITH THE REASON, never dropped (invariant 12): a silent drop is how you learn six months later that one forum's blocks parse to nothing. Four independent defences against the escrow error -- the role label, the inline disclaimer, the GLOBAL stoplist, and shared-service detection over DISTINCT PUBLISHERS -- and they must stay independent: keying the stoplist only on a resolved durable value silently disabled it for exactly the ambiguous lines that need it most, so it falls back to the case-folded observed text. `role` answers WHOSE and the kind columns answer WHAT, and they are separate questions: "Escrow: @forum_escrow" has a known owner and a genuinely ambiguous type. Nothing here writes a binding -- proposals only (invariant 3), always CLAIMED, because docs/10 says only CONFIRMED carries weight and CLAIMED is a lead | Low -- re-parsing is idempotent on (case, raw_sha256) |
| 55 | The ontology is the ONE source of canonical form, and comms had grown a second (2026-07-25, Alembic 0036, apps/api comms.py) | `selectors.py` already stated the rule -- `noctornal_ontology.normalise` is "the ONE source of truth for canonical form" -- and `comms.py` quietly broke it by hand-rolling its own, which then drifted in three places, each silently and each differently. **Matrix** lowercased the WHOLE MXID; the ontology folds only the server part because a localpart is case-SENSITIVE, so `@Alice:x` and `@alice:x` can be two accounts on a historical homeserver. comms gave them ONE durable value and `correlate()` returned them as one actor -- the module written to prevent confident false attribution was manufacturing it. **Tox** disagreed on case only, self-consistently, so nothing broke until something joined a binding to `core.selector` -- which is entity resolution, i.e. the product -- where it would have matched nothing and read as "no correlation". **Telegram** refused a numeric channel id as though it were a `@username`: both a refusal and a wrong explanation. Testing each normaliser against its own expected output could never catch this, because both sides were internally consistent; only COMPARING them does, so that comparison is now a parametrised test over every shared platform. comms keeps its own contribution -- the refusals (SimpleX has no identifier, a `@username` is not durable) and the analyst-facing notes -- and delegates the canonical form. Migration 0036 repairs rows written under the old forms by recomputing from `observed_value`, and its SQL was diffed against the Python normaliser over 17 probes before being trusted, because it is a second implementation of the same rules | High if left -- every durable value is a correlation key, and a re-key invalidates stored bindings |
| 54 | Comms: the DURABLE-SELECTOR mapping is the product, and three platforms are traps (2026-07-25, Alembic 0034, apps/api comms.py) | docs/10: "Read the durable selector column carefully. Getting this wrong is the single biggest source of false attribution in this domain." So `comms.platform.durable_selector_type` is seeded per platform with a NOTE an analyst reads when deciding whether an identifier means what they think it means, and `normalise()` holds the three traps. **Tox**: 76 hex is a 32-byte public key plus 4 bytes of user-rotatable nospam plus a checksum, and actors rotate nospam specifically to shed contacts -- index the first 64. A tool keying on the full 76 silently stops correlating the same actor, and SILENTLY is the problem, because the graph simply shows two people. **Telegram**: the numeric id, never `@username` -- usernames are recycled, so a match can attribute a new person's traffic to an old case, and `normalise` returns NO durable value from a username rather than guessing. **SimpleX**: no persistent identifier exists at all, so it returns None and SAYS SO, because an interface showing nothing implies an absence of ACTIVITY rather than an absence of visibility. OMEMO fingerprints are modelled against a DEVICE node, never an identity: two JIDs sharing one is the same physical device, which is a strong LEAD and not an attribution -- concluding the identities are one person belongs in an ATTRIBUTED_TO edge with a confidence (invariant 2, one level down). CLAIMED / OBSERVED / CONFIRMED are kept apart and CONFIRMED must state its method, because treating a claim as a confirmation is how a rival's Jabber ID gets attributed to whoever posted it as an insult. `provenance_class` is NOT NULL and PERSONA_PARTY must name the persona -- that claim is the one interception law turns on (docs/16 L4) | Medium -- the normalisers are the correlation key; changing one re-keys the graph |
| 53 | Collection: the persona vault has no way to RETURN a secret (2026-07-25, apps/api collection.py) | Invariant 7 said credentials never leave the collector (reworded 2026-09-09 to "never leave the vault": the vault runs inside the API process, decision 9), and the easiest way to hold that is a shape that cannot be misused rather than a rule that must be remembered. `PersonaVault.use()` is a CONTEXT MANAGER and there is no `get_secret()`: a function that returns a plaintext credential is one somebody calls from a request handler, and then the secret is in a traceback, a log line and an error response. `redact()` masks the SHAPE of a credential in every adapter error before it is stored, because a persona's password lands in an HTTP error body far more often than anybody expects and you cannot enumerate the places it will appear. Jitter is SYMMETRIC around the poll interval rather than added to it -- only ever adding makes the minimum gap the interval, which is still a signature a forum admin picks out of an access log. A burn is terminal and needs a reason, because reusing a persona an admin has flagged burns the next one too. RSS is the only adapter, and it REFUSES any feed containing a DOCTYPE rather than trying to neuter one: an XXE in a document written by the people under investigation is a file-read primitive on the host holding every persona credential. SSRF protection is a FLOOR (schemes and private literals) and says so -- DNS rebinding is not addressed | Low -- adapters are pluggable; the vault shape is the part worth keeping |
| 52 | Stealer logs are IN SCOPE by operator directive, and free-text PII search is IMPOSSIBLE rather than forbidden (2026-07-25, Alembic 0033, apps/api ingest.py) | The operator answered docs/12's open question 3 on 2026-07-25. That question said to resolve the compartment and minimisation policy BEFORE writing ingest code; the compartment is resolved in the schema and **the minimisation policy is a legal determination that is NOT resolved** (docs/16 L2, BLOCKING). docs/12: a single archive holds credentials, cookies, tokens and documents belonging to ONE VICTIM WHO IS NOT YOUR SUBJECT, and a feed holds thousands -- it is the most likely route by which this platform becomes a data protection incident. The design principle is docs/12's own: "You can extract almost all of that from the metadata without ever exposing the credential contents. Design for that." So credential VALUES live in a separate table, encrypted, and the analytic path never joins to it. **A permission check gets routed around by the next person who needs a number for a report**, so `victim_credential` has no tsvector, no trigram index and no plaintext column -- there is nothing to run a LIKE against, and a test asserts that by reading `pg_indexes`. Correlation survives through a keyed fingerprint: you must already hold a value to ask whether it appears elsewhere, and even that needs a live two-human time-boxed authorisation, because knowing a specific credential is in the corpus is itself a disclosure. Every control is a column or a CHECK rather than application logic, because a control living only in code is one the next bulk-import script goes around | High -- the compartment and the separation of values from metadata are structural |
| 51 | Break-glass is EASY to obtain and loud in every other way (2026-07-25, Alembic 0032, apps/api break_glass.py) | docs/05: "available, loud and short ... The access is granted; the visibility is what makes it safe." The temptation is to make it hard to obtain and that is the wrong lever -- a break-glass nobody can get at 3am is replaced by a shared password in a drawer, and then there is no record at all. So: refused outright if no active user holds SECURITY_OFFICER (unreviewed emergency access is just access, and a control whose oversight is nominal is worse than none because it produces a record that looks like governance); capped at eight hours by a CHECK, since one that can be granted for a week is a role with a dramatic name; alerting at priority 1, the only tier that overrides quiet hours, so somebody's evening is interrupted on purpose; USE counted separately from grant, because the interesting review case is the grant nobody used; and an unreviewed queue that never ages out, because ageing out is how a review requirement becomes a formality. You cannot review your own. **Deliberately excluded**: break-glass does not cross a compartment -- a compartment is need-to-know and "there is an emergency" is not knowledge of the need | Low |
| 50 | Purge writes a tombstone that outlives the data, and records what actually happened to the BYTES (2026-07-25, Alembic 0032, apps/api retention.py) | docs/08: "the record of destruction survives the data." A purge leaving nothing behind is indistinguishable from data never collected AND from data somebody deleted to hide it. `core.purge_tombstone` is append-only and holds counts, authority and actor but NO content -- one that quoted what it destroyed would be a copy of it. **The unresolved tension is recorded rather than papered over** (docs/16 C2): evidence sits under MinIO COMPLIANCE-mode object lock, which cannot be deleted before its retention expires EVEN TO SATISFY A DELETION ORDER, so `storage_outcome` distinguishes DELETED from LOCKED_UNTIL_RETENTION and nobody tells a court the bytes are gone because a database row changed. Per-category retention may only ever SHORTEN the case clock -- a rule that could extend would let an ingest category outlive the authority the case was opened under. Purge WARNS on any rule no human has confirmed rather than refusing, because refusing would make the first purge the moment somebody discovers the question. **Found while writing the tests: `legal_hold` was on NOTHING** -- docs/08 says it overrides all deletion everywhere and `core."case"` never had the column, so the rule that beats every other retention decision could not be expressed | Medium -- the tombstone shape is what an auditor reads |
| 49 | The report builder redacts STRUCTURALLY, and the document's mark follows its contents rather than the request (2026-07-25, apps/api reports.py) | The tempting implementation searches the finished report for material above the target and removes it. That is not redaction, it is hoping: a name appears in a label, an attribute, a rationale, a selector value, an evidence title and a URL, and a filter that catches five of those has still disclosed. So nothing above the target ever ENTERS the document -- the report is built from a projection computed at the target's clearance, through the same `GraphService` that protects a live analyst. Reusing the enforcement is the point: a redaction routine with its own idea of what AMBER means will eventually disagree with the access gate, and the disagreement gets discovered by a disclosure. **`target_tlp` is a CEILING on inclusion, not the mark.** The mark is derived from what actually went in, so a report requested "up to RED" on a case holding nothing above GREEN comes out GREEN. Over-classification is impossible by construction rather than prevented by a check, which matters because unlike under-classification nothing ever alarms about it. The first cut of this refused to build above the CASE's level, which is wrong -- elements may be classified above their case, and that refusal would have blocked exactly the report that needed to include them. **A report that silently omits is worse than one that refuses**, so every document carries a redaction statement with counts and the sentence that every figure below is a lower bound -- never which classification, which compartment or where (the U2 discipline). **Building and releasing are separate endpoints**: an analyst should see exactly what would leave before anything does, and release calls the egress gate with the DOCUMENT's classification, never the case's, which is the entire reason for building at a lower level. Refusals are audited as loudly as permissions, because an unrecorded refusal is indistinguishable from nobody having tried. The evidence register lists SHA-256 and BLAKE3 per exhibit, because decision 13 targets FRE 902(13)-(14) and CEA ss. 31.1-31.8 and both turn on identifying the record rather than describing it | Low -- the builder reads; it changes nothing |
| 48 | ACH ranks by INCONSISTENCY, never by support (2026-07-25, apps/api ach.py) | docs/13 puts Analysis of Competing Hypotheses in tier 2 because cybercrime attribution is where confirmation bias does the most damage: a team eight months into one theory reads every new post as support. `core.hypothesis` and `core.hypothesis_evidence` had existed since Phase 0 with nothing reading or writing them. **Heuer's central move, which every naive implementation gets backwards: the hypothesis that survives is the one with the LEAST evidence against it, not the most evidence for it.** Counting support ranks whichever theory the team has spent longest collecting for, which is confirmation bias with a scoreboard. Inconsistency is therefore the primary sort, support is reported and never ranks (except to break an exact tie), and a test builds the exact trap -- one hypothesis with the most support AND a contradiction, another with less support and nothing against it -- and asserts the second wins. **Diagnosticity** is the other half: evidence consistent with every hypothesis discriminates nothing, so it scores zero, is flagged, is excluded from the ranking and is kept in the record. Cells are weighted by the Admiralty grading of the assertion behind them, so a "strongly inconsistent" from an F6 source cannot sink a hypothesis the way an A1 does -- with both ramps FLOORED rather than reaching zero, because a zero silently deletes evidence instead of discounting it, and an ungraded source treated as the worst rather than the best, because defaulting upward is how unsourced material acquires authority. Evidence is an assertion and never free text, so every cell inherits a source and a retraction status; a retracted assertion leaves the matrix, the same rule decision 24 applies to the projection. `refute_first` names the most diagnostic UNFINISHED item -- the disconfirming question, not the confirming one | Low -- pure scoring over tables that already existed |
| 47 | Phase 8 (sample handling) is BUILT, on an operator directive that supersedes decision 36's block, and the block is replaced by a runtime REFUSAL rather than by nothing (2026-07-25, Alembic 0031, apps/api samples.py) | The operator directed on 2026-07-25 that Phase 8 be built anyway, with a disclaimer that counsel must review the deployment before it is used in any absolute sense. That is their call and it is recorded as theirs. What decision 36 was actually protecting has not changed: a store of attacker-supplied binaries WILL eventually receive material whose possession alone is an offence, the handling rules differ between the two jurisdictions this build targets (decision 13), and discovering that after the first ingest is a legal problem rather than a technical one. So the block became a refusal with a named condition instead of a refusal with no exit: **nothing ingests until an operator sets `NOCTORNAL_PROHIBITED_CONTENT_POLICY` to a reference an auditor can follow and `NOCTORNAL_DESIGNATED_PERSON` to whoever material is escalated to.** A reference and not a boolean, because "true" is what somebody types to make an error go away and a reference is something they have to possess. It is a DECLARATION, not a verification -- the software cannot check that the policy exists or has been read, and a false declaration produces a working system and an unlawful deployment. Saying so is the most the code can honestly do. **Invariant 10 is enforced at runtime, not documented.** `download()` refuses unless a separate sample origin is configured AND the request arrived at it, because an origin split that is only ever written down does not survive the first hurried deploy -- and serving hostile bytes from the app origin means an escape runs with the analyst's session on the case file, which is a drive-by vector built into the highest-trust system in the estate and seeded with hostile files by design. Also enforced: the object key is the SHA-256 and never the attacker-controlled filename; every sample is encrypted at rest under a per-sample key so nothing in the pipeline is a runnable file (and your own EDR does not quarantine the evidence, which docs/11 calls a routine and embarrassing failure); QUARANTINED is the landing state; bytes are re-verified against their hash on every read and fail closed; REJECTED destroys the bytes AND the data key while keeping the row that says something was rejected and why; a non-private detonation needs a named authoriser in a CHECK constraint; and `MALWARE_ANALYST` grants no case access at all, because being trusted with hostile binaries is a different trust to being trusted with the case file. **Deliberately absent and recorded as absent:** prohibited-content hash screening (no authorised hash set), YARA, ssdeep/imphash/TLSH clustering, archive expansion (an expander without depth and ratio caps is a zip bomb waiting to be sent one), and any sandbox integration. Each gap is written onto the sample row with a reason, because a NULL imphash reads as "this sample has no imports" and a recorded gap reads as "nobody looked", which is the true statement | Medium -- the schema and the refusals are right; the triage depth and the screening are the parts a real lab would have to finish |
| 46 | Notification content is split into THREE fields by where each is allowed to appear, and the email transport structurally cannot reach the third (2026-07-25, Alembic 0029, apps/api notifications.py + transports.py) | docs/07 is specific about email because email is the worst channel available: it sits in inboxes, gets forwarded, and is often synced to phones. Its rule is that the subject line carries no intelligence and the body carries a summary and a deep link, not the content. That rule is unenforceable if a notification is one blob of text, because whoever writes the transport then has to REMEMBER which parts are safe. So the contract is in the schema: `subject` never carries intelligence (it renders on a lock screen), `summary` carries no entity names or selectors (it may be emailed), `body` may name entities and is in-app only. `Outgoing` -- the row the transport receives -- does not carry `body` AT ALL, so `render_email` could not leak it if it tried, and a test asserts that shape rather than trusting a comment that survives exactly one refactor. The sharp case is a merge: shadowbroker merged into A. Petrov is exactly the sentence the case owner needs and exactly the sentence that must not leave the building. **Content that cannot egress is neither dropped nor downgraded.** The delivery row is REFUSED with the gate's reason code and a content-free stub goes out instead -- not even the case code, because a code like OP-KESTREL on a lock screen tells a shoulder-surfer that an operation by that name exists and that this person works on it. The full notification stays in the centre behind the access gate, which is where it should have been read anyway. **A notification is classified data**, so each row carries its own classification and compartments, and the inbox is filtered by the reader's CURRENT clearance in SQL -- a revoked clearance has to hide old notifications too, or the centre becomes a retention loophole for everything the analyst used to be able to see. Rows are also never WRITTEN for a recipient who could not read them. **Quiet hours and digest defer, they never drop**, and priority 1 ignores both -- which is the only thing that makes silencing a channel overnight safe. **Never tell someone what they just did** is the fourth suppression, and it is the single commonest reason people mute a notification system. **Deep links carry no token**, contrary to docs/07's suggestion: a single-use token in an email is a bearer credential in the least trustworthy channel in the system, and requiring the recipient to authenticate is both simpler and stronger. **No worker.** `dispatch_due()` is a function an operator, a cron entry or a test calls, following decision 30's precedent -- and that limitation is written down rather than hidden behind a thread that dies silently at 3am. Jira and the integration admin surface are NOT built | Medium -- the three-field split reaches every call site that raises a notification |
| 45 | Adversarial review of the rate limiter found 12 real defects, one of them CRITICAL, and the fixes are in decision 43's implementation (2026-07-25, 31 agents, 27 findings, 15 refuted) | The pattern from every previous review pass held: reviewers told to BREAK the thing found what reviewers told to check it would not. **(1) CRITICAL -- the blanket ceiling was not a ceiling.** It was keyed only on the presented credential, and nothing validates that a bearer token is a live session, so a caller rotating a random token per request minted a fresh empty meter every time. Reproduced against the real app: with the limit at 3, a fixed token gave 27 refusals in 30 requests and a rotating one gave ZERO. Worse, the bearer branch returned early and suppressed the address fallback, so sending one garbage header made a caller strictly LESS limited than sending none -- the control inverted. It was also the only limiter on roughly 40 of 45 routes. Fixed by adding `request.source`, keyed on the peer address, which a caller cannot mint: the credential meter now subdivides a budget it cannot leave. **(2) Every IPv4-mapped address shared one bucket.** `::ffff:a.b.c.d` all have the /64 `::/64`, and a dual-stack listener reports every IPv4 peer that way -- so on a default deployment the first person to mistype a password would have locked out the internet, while the limiter looked like it was working. Unmapped now. **(3) The audit throttle failed OPEN.** An unreachable backend is the same condition that makes every DENY-policy limit refuse every request, so the throttle would have turned each refusal into a serialised undeletable append-only write -- the outage authoring the exact flood the throttle exists to prevent. Fails closed now; the outage is already logged by `check()`. **(4) The blanket check blocked the event loop.** An `async def` middleware calling the synchronous redis-py client does socket I/O on the loop thread, so one slow Redis serialises every request in the process and the 250ms timeout costs 250ms of loop time per request -- which defeats the entire point of that limit failing OPEN. Now `run_in_threadpool`, asserted by a test that records which thread the backend ran on. **(5) X-Forwarded-For read only the FIRST header line.** RFC 9110 makes repeated field lines equivalent to one comma-joined value, and HAProxy's `option forwardfor` emits a separate line by default -- so `parts[-hops]` indexed a list the client authored in full while the proxy's real entry sat unread. All lines are joined now. **(6) `/graph/metrics` was unmetered** behind the same `analytics.run` permission as the metered suite, with comparable igraph work and no result cache: the analytics door was locked and the window was open. It now shares `analytics.suite`'s budget, because two doors onto one cost with two budgets is one budget that means nothing. `/graph`, `/graph/ego`, `/graph/path` and `PUT /graph/layout` are metered too, and `LayoutBody.positions` is bounded. **(7) Per-endpoint headers vanished on any handler returning a Response** (evidence export and download both do), because FastAPI discards `sub_response` there -- and the middleware then advertised the blanket ceiling, so the client was told the WRONG number rather than none. The decision is stashed on `request.state` and the middleware writes it. **(8) A successful login advertised the failure meter** it had not spent, because a peek guard wrote the same header names as the consuming limit. Peeks emit no headers now. **(9) The login peek/consume race** is real and is now BOUNDED rather than closed: the failure meter is read before the Argon2id verify and consumed after, so a simultaneous burst all read an un-advanced meter. The attempt burst was cut from 60 to 20 so one burst cannot buy more guesses than the failure meter allows in a whole window, and a test asserts that relationship. Every one of these has a regression test named for the defect | Low -- all fixes are inside the limiter and its wiring; nothing outside changed shape |
| 44 | Four-eyes approval is a GENERIC mechanism built before the operations that need it, and dual control on merge is a PER-CASE switch defaulting to OFF (2026-07-25, Alembic 0028, apps/api approvals.py) | docs/05 asks for dual control on "the genuinely irreversible: case deletion, evidence purge, role definition changes, persona credential reveal -- two distinct humans, enforced by constraint"; docs/08 adds out-of-schedule purge; docs/04 adds credential reveal. **None of those operations is built yet**, and the mechanism is built first anyway, because retrofitting a second approver onto an operation that already ships is how you get dual control that is really single control with a form to fill in. Four properties make it a control rather than a ceremony. **(a) The approval binds to the exact parameters.** `payload_hash` covers the operation, the case AND the canonicalised payload, and consuming re-computes it. Without that binding, "Bob approved it" means Bob approved whatever the requester did next -- get a nod for merging two obviously-identical spam bots, then post the ids of the two nodes the case actually turns on. Under dual control the merge endpoint therefore reads ONLY `approval_request_id` and executes the parameters recorded on the approval; the request body is ignored, so there is nothing to substitute. **(b) Single use, by state machine.** `consume` is one atomic `UPDATE ... WHERE state = 'APPROVED'`, so two concurrent attempts cannot both win, and the consume shares a transaction with the operation -- split them and a crash in between either burns an approval without doing the work or does the work without burning the approval, and the second is the bad direction. **(c) Expiry is COMPUTED, never swept.** There is no EXPIRED state and no job that sets one; `expires_at` is compared at decision and consumption time. A control whose correctness depends on a scheduled task fails silently the first time the scheduler does. **(d) Two distinct humans, in the DATABASE.** `approval_two_distinct_humans` is docs/05's phrase as a CHECK constraint, because application code gets refactored around and a constraint does not. It cannot stop one person with two accounts -- that is a joiner/leaver problem -- but it does stop the far commoner accident of a UI that lets the requester click their own Approve button. The approver must ALSO independently hold the operation's own permission and have a fresh second factor: an approver drawn from a wider pool than the actors is a rubber stamp with a job title, and one whose session has been unlocked since this morning is not even that. **Merge specifically is opt-in and defaults to off.** docs/05 scopes dual control to the genuinely irreversible, and a merge here is the most reversible destructive-looking operation in the system -- a ledger with an exact restore (decision 41). Entity resolution is also the daily work of this tool, so a second signature on every merge in a case with three hundred personas is a control that gets switched off in week two and never switched back on. It is therefore one per-case switch (`case.dual_control_merge`), audited in both directions, for the unit whose standing orders require it. Reversal is deliberately NOT dual-controlled even when merging is: undoing a merge restores the pre-merge state, and requiring two humans to correct a mistake is how mistakes stay in a case file | Low -- the operations that will need it unconditionally are not built yet, so their wiring is still free |
| 43 | Rate limiting: GCRA in Redis, and the fail-open question answered PER LIMIT rather than globally (2026-07-25, apps/api ratelimit.py + ratelimit_redis.py + http/limits.py) | decision 30 shipped Phase 3 analytics with an accepted caveat -- a CPU-bound igraph path behind a non-step-up permission with no rate limiting -- and docs/05 asks for "per-user and per-endpoint rate limits; hard limits on export and search". Four choices are load-bearing. **(a) GCRA, not a counter.** A fixed window lets 2x the quota through across a boundary, which on the analytics path is 200 igraph runs in two seconds; a sliding-window log makes the memory cost of limiting an attacker scale with the attack. GCRA stores ONE integer per subject and yields an exact Retry-After -- and, critically, a DENIED request does not advance the meter, so hammering cannot extend your own lockout and a forged subject cannot extend someone else's. **(b) Fail-open vs fail-closed is per limit, not global.** Every cost-bearing or security-sensitive limit (login, analytics, export, ingest, capture, merge, search) fails CLOSED with a 503: a limiter that fails open is an availability-triggered bypass, and everything else in this codebase -- access resolution, the evidence hash re-verify, the ingest key scope check -- already fails closed. The ONE exception is the blanket per-credential ceiling applied by middleware, which fails OPEN, because denying that turns a Redis restart into a total outage of an investigation tool. A Redis outage therefore degrades the expensive features and leaves the case file readable. A test asserts that `request` is the only fail-open entry, so a later edit cannot quietly add a second. **(c) Login is metered TWICE, and the split is the point.** An IP-scoped limit on login ATTEMPTS cannot be tight, because the customer is an organisation behind one egress address -- two hundred analysts signing on at 09:00 is two hundred logins from one IP, and a limit tuned to stop spraying takes that unit offline every Monday. So the attempt limit is generous (120/min) and exists only to cap one source's draw on a deliberately expensive Argon2id verify. The anti-guessing control is a SECOND meter that counts FAILURES (30 per 5 min), peeked before the hash runs and consumed only when authentication actually fails. Successes never move it, so a building full of correct passwords is invisible to it and a sprayer produces nothing else. Neither is email-scoped: anyone can send a given address, so an email-keyed limit is a remotely triggerable lockout of a named analyst -- the same defect class the decaying account lockout already fixed once. Honest gap: both are per-source, so a botnet at one guess per address per hour moves neither. **(d) Denials are audited at most ONCE per window per subject.** `audit.event` is append-only and hash-chained and its chain trigger takes an advisory lock, so rows serialise -- auditing every denial would let a caller who is already throttled convert rejected requests into unbounded, undeletable, serialised writes, handing an attacker a better denial of service than the one being blocked. Also decided: time comes from `redis.call('TIME')` so several API processes cannot each enforce their own clock (this development host's clock is unsynchronised -- docs/15); one Lua script serves both peek and measure, switched by an argument, so they cannot drift; X-Forwarded-For is IGNORED unless `NOCTORNAL_TRUSTED_PROXY_HOPS` is set, and then read from the RIGHT, because a client-authored subject key lets an attacker both escape their own limit and exhaust a victim's; IPv6 is metered by /64, since limiting a single /128 limits one address out of the 2^64 the same host already holds; and without `REDIS_URL` the limiter runs per-process and says so loudly rather than silently doing nothing | Low -- the compute is pure and the backend is one Protocol with two implementations |
| 42 | CI runs four gates, and the lint rule set is deliberately SMALL (2026-07-25, .github/workflows/ci.yml, ruff.toml) | Phase 0 listed "lint, typecheck, test, migration round-trip" and all four were run by hand for the whole build (docs/14 O1). The jobs are split by what a failure MEANS -- lint (untidy), source hygiene (corrupted), ontology drift (generated files no longer match their source), tests+migrations (the product is wrong) -- so a red tick says which happened. Ruff selects E/W/F/B only: I (isort) and UP (pyupgrade) were tried and dropped after they wanted 48 changes that would not have caught a single defect, and B008 is ignored because FastAPI's entire dependency system is `Depends()` in a default argument, so it fired 157 times on correct code. A linter that is wrong about the framework in use trains people to ignore it. Two gates encode hazards this repo actually hit: the migration round-trip runs head->base->head because 0026 originally only applied to an empty table, and `check_source_hygiene.py` rejects NUL bytes (docs/15: the editing tools have corrupted non-ASCII literals into them, which serves as a blank page with nothing in the console) and Trojan-Source bidirectional characters. CI also fails if any test SKIPPED, because every _pg test degrades to a skip without DATABASE_URL and a dead service would otherwise show as green | Low |
| 41 | Entity merge is a LEDGER, not a flag, and never crosses the persona/person boundary (2026-07-25, Alembic 0027, apps/api merges.py) | docs/01 opens its entity-resolution section with "merging is the operation most likely to quietly corrupt a case" and requires that "its edges are re-pointed with a record of the original endpoints". `core.node.merged_into_id` had existed since 0005 with nothing writing it, because a merge is not one column: re-pointing destroys the original fact, so `core.node_merge` + `node_merge_edge` record every moved endpoint and reversal is a RESTORE rather than a re-derivation -- working out where an edge "should" go after the fact is the same guesswork that made the merge wrong. A tie BETWEEN the merged pair would become a self-loop, which core.edge forbids, so it is recorded and soft-deleted and comes back on reversal. A partial unique index allows re-merging after a reversal while forbidding two live merges of one node. Reversed merges stay in the history, because a merge that vanished would hide that somebody once believed these were the same actor. IDENTITY may never merge into PERSON: that is an ATTRIBUTION carrying a confidence (invariant 2), and a merge asserts the records ALWAYS described one thing. Step-up gated per docs/01, via a new composable `require_step_up` dependency; the case-owner notification docs/01 also asks for is Phase 5 and is NOT built | High if the ledger is dropped later -- the reversal data has nowhere else to live |
| 40 | Manual capture before adapters, and it is the FIRST producer of proposals (2026-07-25, apps/api extraction.py) | docs/14 C2 recommends exactly this: "a paste-a-conversation-export path that lands a document, extracts selectors with offsets, and proposes graph changes would exercise the whole proposal pipeline without any of the persona-management risk". Phase 4's adapters are a large build with real operational hazard (personas, egress binding, FLOOD_WAIT, parser drift) and none of it is needed to prove the part that matters. Text lands as a `collect.document` deduped on content hash; regexes produce `collect.extraction` rows WITH character offsets; each new value becomes a proposal carrying the matched span and its surrounding sentence, because a handle lifted out of a quoted signature block looks identical to a real one until you see the context. Deliberately proposes SELECTOR nodes only -- never PERSON, because attribution is an assessment a human makes (invariant 2). The document is deduped GLOBALLY but proposals are per-case: the same thread pasted into a second case must still raise proposals there. Scores state how often the PATTERN is wrong in prose, not how important the finding is | Low - the extractor is replaceable; the pipeline it feeds is the durable part |
| 39 | Proposal pipeline: invariant 3 enforced by CLASS SHAPE, not by discipline (2026-07-25, apps/api proposals.py) | `collect.proposal` had existed since Phase 0 with nothing writing it, so "machines propose, analysts dispose" was true by accident rather than construction -- the only one of the three founding ideas with no enforcement. Split in two: `ProposalStore` is the extractor-facing half and holds NO GraphWriteService, so it is physically unable to reach `core.node`/`core.edge`; `ProposalReview` is the only path into the graph and requires a human `reviewed_by`. Accepting applies through GraphWriteService, so the element and its assertion commit together (invariant 1) -- an accepted proposal is an analyst making a claim, not a back door. The assertion is basis AUTOMATED_INFERENCE at LOW confidence carrying the extractor's name and rationale, and an accepted EDGE is born `is_inferred` (invariant 4), so accepting a suggestion cannot silently move anyone's centrality. A DISPUTED state exists because a queue whose only options are yes and no forces a decision on ambiguous items, which is how junk gets accepted. No HTTP endpoint CREATES a proposal: they come from extractors inside the boundary | Medium - the review semantics are load-bearing for Phase 4 |
| 38 | One destination-aware egress gate, and evidence now goes through it (2026-07-25, apps/api egress.py) | docs/07 opens by requiring "one function, `can_egress(object, destination)`, called by SMTP, Jira, webhooks and export alike", because integrations are the leak path in every system of this kind -- a Jira ticket auto-created from a watch hit quietly copies intelligence into a system with a different access model and a wider audience. Evidence carried its OWN frozenset of non-egressable classifications; a second copy is how copies drift, and the one that drifts is the leak. The gate is pure (labels in, decision out), so it is exhaustively testable, and it fails CLOSED on an unknown classification, an unknown destination or an unparseable ceiling. A per-destination ceiling can only ever LOWER what is permitted -- the AMBER_STRICT/RED floor is checked first so no integration config can argue past it. Compartmented material never egresses at all: no external system models compartments, so sending it would silently drop the control rather than enforce it elsewhere. The message keeps the literal phrase "invariant 8" because tests assert on it | Low - additive, and it removed a duplicate rather than adding one |
| 37 | Layout: hand-written ForceAtlas2 + Barnes-Hut in a Web Worker, NOT sigma.js/graphology (2026-07-25, static/layout-worker.js) | docs/02 asks for "ForceAtlas2 with Barnes-Hut in a worker" and docs/14 U1 records the blocker: the UI ships under `script-src 'self'` with no bundler and no inline script, so an npm dependency means adopting a build step. That is a real decision and should not arrive as a side effect of wanting a layout. A worker is a same-origin script file, so it needs neither. Barnes-Hut takes repulsion from O(n^2) to O(n log n); measured 400 nodes / 1,187 edges in ~1s off-thread, and cluster separation improves 1.1x -> 9.3x by 400 iterations on planted-cluster fixtures. The main-thread spring loop is KEPT for interactive drag, which wants immediate local response. Progress posts are rate-limited to ~10/s and repaints coalesced onto animation frames | Low - swapping in sigma.js later is a renderer change, and the bundler decision stays open. sigma.js is not in the tree and never was (recorded 2026-09-09) |
| 36 | NO prohibited-content policy exists, and Phase 8 (sample handling) MUST NOT ingest until one does (2026-07-25) | docs/09 Phase 8 says "Decide first: the prohibited-content policy, with counsel, before the first ingest rather than after." It has not been decided and is not something this build can decide for itself: a malware/sample store that accepts arbitrary attacker-supplied binaries will eventually receive material whose mere possession is an offence, and the handling obligations differ by jurisdiction (the deployment targets Canada AND the US, decision 13). Recorded in the README as an explicit blocker rather than left implicit in a roadmap footnote. Phases 0-7 are unaffected -- none of them stores a sample | Very high if ignored - the cost of discovering this after the first ingest is legal, not technical |
| 35 | Phase 7 comms: MESSAGE-LEVEL capture, not metadata-only (2026-07-25, operator decision) | docs/09 asked the question and noted metadata is "dramatically cheaper and carries most of the analytic value". The operator chose message-level anyway, which is the defensible call for a law-enforcement-primary deployment (decision 16): message CONTENT is what a disclosure obligation and a prosecution actually turn on, and a metadata-only capture cannot be re-run against a channel that has since been deleted. The costs are accepted and real: storage grows with traffic rather than with the number of parties, every captured message is personal data inside the retention and minimisation regime, and the TLP egress gate (Phase 5) becomes load-bearing rather than advisory | High - re-capturing content that was only ever seen as metadata is usually impossible |
| 34 | Retraction is enforced in the projection AND exposed in the UI; provenance strength is drawn on the canvas (2026-07-25, enhancements E1-E3) | The first real session produced 14 assertions and ZERO exhibits, and retraction -- the operation that makes the assertion model load-bearing -- was reachable from no interface at all. Now: an assertion can carry its exhibit at the moment the claim is made (`evidence_id` had existed unused since Phase 1), the projection reports `has_evidence` per element, unevidenced entities render with a hollow core and unevidenced ties render faded, and case-level evidence coverage is a headline number that reads red at zero. Retracting the last live assertion visibly dissolves the element from the live graph. An exhibit may only support a claim in its OWN case | Low - additive |
| 33 | Two-mode projections are WARNED about, never silently rewritten (2026-07-25) | docs/03 says CONTROLS "will wreck centrality if included", yet the Financial preset includes CONTROLS/TX_INPUT/TX_OUTPUT (making WALLET and TRANSACTION vertices) and Communication includes PARTICIPANT_IN (making CONVERSATION one). Centrality over that answers a different question: an artefact with several controllers scores as a broker. Editing PRESETS would silently change every number the Phase 2 sociogram already shows, so the analytics response carries a `mode_warning` naming the non-actor types instead, distinguishing ones that carry ties (distort brokerage) from isolates (only inflate the percentile denominator). Proper bipartite projection to one-mode with Newman weighting is still open. **Answered 2026-09-24 for forums and wallets (decision 73)**, as a per-run option that leaves the presets and this warning as they are; conversations stay with the Comms pane's co-participation projection (decision 58) and are not projected in the Analysis pane | Low - the warning is additive; a real one-mode projection is a new parameter |
| 32 | Live-assertion filter implemented in `GraphService.project()`, completing decision 24 (2026-07-25) | Decision 24 scoped LIVE provenance as "a PROJECTION property, NOT write-enforced" so a retracted element could dissolve from the live graph while its rows survive for temporal replay. The projection never implemented it, so retraction was cosmetic: the UI showed a RETRACTED chip while the node kept its full degree, and Phase 3 would have computed betweenness and takedown targets over withdrawn evidence. Both SELECTs in `project()` now require `EXISTS (... assertion WHERE retracted_at IS NULL)`. Zero assertions were retracted when this landed, so no existing number moved | Low now; the cost was in leaving it undone |
| 31 | A metric run is cached against the CALLER'S VISIBILITY, not just the graph (2026-07-25, Alembic 0026) | `project()` filters by the caller's clearance and compartments, so the same question over the same case yields different graphs per analyst. A cached betweenness computed over RED nodes served to an AMBER analyst would hand them a score whose entire explanation is a node they may not see. Two independent mechanisms: `graph_hash` is taken over the caller-VISIBLE node and edge lists, AND the lookup filters on `visibility_clearance` / `visibility_compartments`. Both columns are NOT NULL with NO DEFAULT deliberately - `'{}'` is what an analyst holding no compartments looks up with, so a default would make a forgotten write fail OPEN. Cost: two clearances that happen to see an identical graph each compute their own run | Low - stricter than needed, and the strictness is the point |
| 30 | Phase 3 analytics run SYNCHRONOUSLY in the API process, not in a separate worker (2026-07-25, apps/api analytics.py + analytics_runs.py) | docs/02 puts "analytics workers (igraph)" in Zone B, but a queue adds a process, a client dependency, a progress-reporting UI and a new failure mode without changing a single number at the scale this tool has. docs/03's own band is "< 5k nodes: exact, everything, synchronous, sub-second", and measured: 2,000 nodes / 6,000 edges is 1.15 s for the whole suite. The compute layer is nevertheless written worker-ready - `analytics.py` is pure and database-free, `analytics_runs.py` owns all persistence, so moving to a worker (the sketch's Arq/NATS, both removed from the plan, compose R13 2026-07-26) is a change of caller, not of algorithm. Caps enforce the band: betweenness switches to Brandes pivot sampling above 3,000 nodes, key player refuses above 5,000 or on a truncated projection. ACCEPTED CAVEAT: this is a CPU-bound path behind a non-step-up permission with rate limiting still deferred (decision 29), so it is a DoS surface for an authenticated, case-assigned analyst | Medium - the seam is deliberate, but a worker still needs a queue, a runtime and progress UI |
| 29 | HTTP API layer, hardened after adversarial review (2026-07-25, apps/api/http/) | FastAPI under `/api/v1`, problem+json, security headers; every case-scoped endpoint depends on `require()`/`authorize_object` so authorization is decided once. Review found and fixed: **effective labels** (an element is protected by the STRICTER classification and the UNION of compartments with its case, uploaded evidence has empty compartments, so the need-to-know leg was passing vacuously); **the case listing** now applies verb+lattice+compartments in SQL (it applied none of them); **search** filters by the CALLER's ceiling, so an over-classified label/title is invisible rather than discoverable-then-403; **write ceiling** (you cannot author content above your clearance); **no raw psycopg text or submitted input** in responses (a 422 was echoing the submitted password and live TOTP code); **CSRF** double-submit for cookie-authenticated unsafe methods; **logout revokes one session**, not all; **lockout decays** (it never did: one bad login every 15 min locked an analyst out forever); **auth/authz events audited**; existence oracles closed (no-relationship ⇒ 404, same as nonexistent). DEFERRED and documented in apps/api/README.md: rate limiting, session IP/UA binding, login timing equalisation, compartment registry | Medium, endpoint wiring is where authz regressions hide, hence the e2e suite |
| 28 | Tags, node sets, full-text search (2026-07-24, Alembic 0025, apps/api curation.py) | Tags = controlled vocab (namespace+name), case-scoped or global (case_id NULL), hierarchical (parent_id), external_id for MITRE; unique per (case,namespace,name) or global. Node sets = ad-hoc analyst working sets, deliberately NOT edges (so they never distort centrality, docs/01). Search over the trigger-maintained tsvectors: nodes (label+attrs, excludes soft-deleted/merged) and evidence. Migration 0025 added the MISSING evidence search_tsv trigger (title+description+extracted_text, 500k-capped), evidence had the column + GIN index (0008) but nothing populated it, so evidence search silently returned nothing until now | Low |
| 27 | Case CRUD service (2026-07-24, apps/api cases.py) | Creating a case atomically grants its owner (and deputy) CASE_OWNER access: the five-part gate reads roles off case_assignment, so without this the owner couldn't act on their own case. Validated status lifecycle (DRAFT→ACTIVE→DORMANT↔ACTIVE→CLOSED→ARCHIVED→PURGED; illegal transitions rejected; closed_at stamped). Governance guards beyond the schema: lawful basis non-empty, review_due≤retention_until, and owner/deputy clearance ≥ case classification (a sub-cleared owner could never see their own case, caught at creation). Every create/edit/status/grant/revoke writes a hash-chained audit.event. Owner access cannot be revoked | Low |
| 26 | Evidence: WORM storage + dual-hash + tamper-evident custody (2026-07-24, Alembic 0023-0024, apps/api evidence.py) | Prosecution-grade (decision 13), hardened after an adversarial review. Bytes → MinIO object-lock in **COMPLIANCE** mode (not GOVERNANCE, not even root can delete/overwrite before retention; true WORM). SHA-256 **and** BLAKE3 computed from original bytes at ingest; read-back verified after PUT; **every read (view/export) recomputes and fails closed** on mismatch, so a swapped object version cannot be served with a clean log. Custody ledger is append-only (0023) AND hash-chained with server-pinned occurred_at + actor FK (0024), back-dating/forged-actor/deletion all detectable. Every touch also writes a hash-chained audit.event; linking is audited (EVIDENCE_LINKED); dedup still records custody. export() enforces the invariant-8 floor (AMBER_STRICT/RED never leave the boundary). DEFERRED: full destination-aware egress gate (Phase 5); API should run as a non-owner DB/MinIO role in production (privilege separation) (the hash chain is the detection backstop until then; object-versionId pinning is a further hardening | High) chain of custody cannot be reconstructed after the fact |
| 25 | Selector storage wired to the ontology normalisers (2026-07-24, apps/api selectors.py) | core.selector is the entity-resolution join key; every write runs raw→norm through noctornal_ontology.normalise (the ONE source), so the DB norm_value cannot drift from the ontology. UNIQUE(case,type,norm) ⇒ a selector is one row/owner per case, so "same observable, two owners" is only expressible CROSS-case; a within-case merge lead is instead an attribution conflict at link time (strong selector already owned ⇒ SelectorOwnerConflict, force to repoint). node_id is observation bookkeeping, not an asserted edge (assert ownership via a CONTROLS edge). Cross-case pivot requires the caller's allowed_case_ids, so it cannot leak past open question 5. No schema change (table exists since 0005) | Low |
| 24 | Assertion layer enforced in the database, steady-state (2026-07-24, Alembic 0022) | Invariant 1 is a SYMMETRIC pair of DEFERRABLE INITIALLY DEFERRED constraint triggers: (1) AFTER INSERT on node/edge (no element commits without an assertion row; (2) AFTER DELETE OR UPDATE OF node_id/edge_id on assertion) the LAST assertion of a still-existing element cannot be deleted/repointed. Trigger 2 closes the two paths trigger 1 alone missed (SET CONSTRAINTS ALL IMMEDIATE timing game; later-transaction delete), an adversarial review found both. The guarantee is thus ">=1 assertion ROW per element at ALL times", via any write path. GraphWriteService is the atomic API on top. SCOPE (deliberate): LIVE provenance (>=1 non-retracted assertion) is a PROJECTION property, NOT write-enforced, because retraction propagation (docs/01) requires an element to lose all live support and dissolve from the live graph while its row + history persist for temporal replay. Per-attribute provenance (claim_path) is supported but is application discipline | Cannot be retrofitted: the one GETTING-STARTED says never skip |
| 23 | Role→permission matrix seeded (2026-07-24, Alembic 0021) | Roles and permissions existed but were unlinked, so the verb check had nothing to read. Starting matrix (58 grants): SYS_ADMIN/SECURITY_OFFICER hold NO case-content permissions (separation of duties); CASE_OWNER full incl. grants + dual-controlled destructive verbs; ANALYST creates/edits but no grant/review/export; REVIEWER approves not originates; CONTRIBUTOR uploads not accepts; READ_ONLY/LIAISON view only. Change via role.manage + migration | Low, data, re-seedable |
| 22 | Orphan node types wired in; TRANSACTION = proven criminal on-chain tx (2026-07-24, Alembic 0019) | TRANSACTION gains TX_INPUT/TX_OUTPUT wallet legs (two-mode money graph; PAID stays the actor summary); DATASET/CREDENTIAL_SET gain CONTROLS (held-by) and EXFILTRATED_FROM (breach provenance to victim). All structural. Resolves open question B. 49 edge types now | Low, new edges, no rework of existing data |
| 21 | Structural edges out of the social projection; SAME_AS layer-gated (2026-07-24) | `PARTICIPANT_IN`, `SAME_DEVICE_AS`, `CO_POSTED_IN`, `SHARED_INFRA` are bipartite/plumbing edges, counting them social double-counts affiliation or fabricates alliances (two rivals on one bulletproof host are not friends). The edge validator now rejects SAME_AS crossing the IDENTITY/PERSON layer: attribution is exclusively ATTRIBUTED_TO (invariant 2) | Medium, projections built on the old flags would need recomputing |
| 19 | Stealer logs in scope but SEGREGATED, never inside the core evidence store (2026-07-24) | Stealer logs are bulk third-party PII, the most likely route to a data-protection incident. They upload to a samples-style segregated environment (separate origin/bucket/compartment, docs/11 model): metadata and extracted selectors may flow to the graph as assertions; raw dumps never enter `core.evidence`. PII masking and minimisation land before any stealer ingest code | High, once raw dumps sit in the case store, retention/disclosure obligations attach and cannot be unwound |

---

## New open questions raised in session 2 (ontology)

**B. RESOLVED 2026-07-24 (decision 22).** TRANSACTION, DATASET and
CREDENTIAL_SET are wired into the edge vocabulary (Alembic 0019).
TRANSACTION is a *proven criminal on-chain transaction*: wallets are its
inputs (`TX_INPUT`) and outputs (`TX_OUTPUT`), so the money graph stays
two-mode and identities reach it through `CONTROLS`→wallet; `PAID`
remains the actor-level summary edge and the two coexist. DATASET and
CREDENTIAL_SET can be held by an actor (`CONTROLS`) and carry breach
provenance to a victim (`EXFILTRATED_FROM`). All three new edges are
structural (not social ties).

**D. Two-step MFA UX needs an opaque ticket.** Session-3 auth is
deliberately single-step: the client submits password AND TOTP together
and gets one generic INVALID_CREDENTIALS for any wrong factor. A
"password first, then prompt for code" UX would leak password validity
(a free, lockout-free password oracle) unless the transition is gated by
a short-lived opaque MFA ticket issued indistinguishably whether or not
the password was correct (identical response, identical Argon2 work, a
lockout increment either way). Build that ticket flow before offering a
two-step login. Found by the session-3 adversarial review.

**A. Venue-scoped FORUM_UID.** A forum UID is unique per forum, not
globally; unscoped it is a wrong-merge factory, so it is currently weak.
To make auto-merge safe enough to build (it never was) we need the
ingest layer to produce a venue-scoped
norm form (e.g. `<forum-slug>:42`), which means selector matching gains a
venue dimension. Decide when the collection layer is designed.

**B. TRANSACTION / CREDENTIAL_SET / DATASET are orphan node types.** They
are decided node types but appear in no edge type's endpoints, so a node
of one of these kinds cannot currently be connected to anything. Decide
which edges they belong on (e.g. TRANSACTION alongside PAID, DATASET/
CREDENTIAL_SET as objects of SOLD_TO / CONTROLS) before graph-authoring
UI lands, deferred to keep this pass to verified fixes only.

**C. IDENTITY-target BANNED_BY in the signed projection.** A moderator
banning a user is a negative trust signal (docs/01), but BANNED_BY also
targets venues (FORUM/CHANNEL) which are not. The single is_social_tie
flag cannot split by endpoint; resolving this needs the endpoint-aware
projection rule planned for Phase 2 analytics.

## Open questions: worth answering before Phase 1

These are the places I made a call without enough information. Each one is
cheap to change now and expensive later.

**1. Jurisdiction and operating context.**
Are you law enforcement, a private CTI vendor, a CERT, or in-house
security? This changes the legal basis vocabulary, the retention defaults,
whether disclosure packs matter at all, and how conservative the collection
defaults should be. I have defaulted to something that would satisfy a
European LE or a regulated private vendor, which is the strictest common
case, easy to relax, hard to tighten.

> **Answered 2026-07-24:** jurisdiction is **Canada + US**; operating
> context is **law enforcement primary, private CTI secondary**
> (decision 16). Legal-basis vocabulary and disclosure features target
> LE; collection defaults stay conservative.

**2. Will this data ever go to a prosecution?**
If yes, the WORM evidence store, custody ledger and disclosure pack are
load-bearing and should stay in the MVP. If it is purely internal
intelligence that will never be evidence, you can defer chain of custody
and save real time in Phase 1.

> **Answered 2026-07-24: YES, prosecution-grade, Canada + US** (decision
> 13). WORM store, custody ledger and hash verification stay in Phase 1.

**3. Expected case size.**
My assumption: hundreds to a few thousand nodes per case, tens of thousands
at the extreme. If you expect hundreds of thousands in a single case, the
rendering and analytics strategies both change, server-side layout and
level-of-detail rendering become necessary rather than optional.

**4. Team size and concurrency.**
Under ten analysts and you can skip real-time collaborative editing and use
optimistic locking. Above that, you need presence and conflict resolution
on the graph, which is a substantial piece of work best planned for now.

**5. Cross-case pivoting.**
Should an analyst see that a wallet in Case A also appears in Case B they
have no access to? A "there is a match, request access from the owner"
signal is enormously valuable and also a compartment leak. My schema
supports it (selector indexes are global), but I have not decided the
policy. This is a genuine tradeoff and needs a human answer.

**6. Deployment environment.**
Air-gapped, on-prem, or cloud? Air-gapped kills the collection layer and
changes everything about how updates and threat feeds work. Cloud raises
questions about where evidence physically sits.

**7. Existing Jira instance.**
Cloud or Data Center? Cloud means webhook-based inbound and OAuth 2.0 3LO.
Data Center means you can be more relaxed about network position but
webhooks need a reachable endpoint. You mentioned prior work here, so you
may already have the pattern.

> **Answered for outbound use 2026-09-25 (decision 122):** Jira is an
> opt-in outbound channel to one destination, Cloud or Data Center
> (`NOCTORNAL_JIRA_NETWORK` names a Data Center's private network), with
> a sealed credential. Nothing comes back in: there is no inbound webhook
> and no OAuth flow, and Jira is never authoritative. A Cloud
> destination's residency and audience are docs/16 C17.

**8. Do you need Telegram at all in the MVP?**
It is by far the highest-effort and highest-risk adapter, persona
management, FLOOD_WAIT, session security, overt join events. If forums
carry most of your value, RSS plus XenForo gets you a working pipeline in
half the time and proves the model before you take on MTProto.

> **Answered 2026-07-24:** evidence **capture** yes, automated
> **monitoring** no (decision 18). Analysts file Telegram exports as
> evidence with full custody; the MTProto monitoring adapter stays
> deferred and monitoring is handwork for now.
>
> **Answered again 2026-09-25:** monitoring is built (decision 134), on
> MTProto with Telethon, and only behind a confirmed two-person collection
> authority (decision 69), the egress proxy (decision 107) and a declared
> ceiling capped at AMBER (decision 103). The capture path above is
> unchanged. What the adapter assumes about Telegram is in docs/16 C18,
> and it has not yet been run against Telegram itself (docs/17 F31).

**9. Language coverage.**
Russian-language forums are central to this domain. Do you need
translation, transliteration handling for handles (Cyrillic/Latin
homoglyph confusion is a real attribution problem), and Cyrillic-aware
stylometry? This affects the extractor and embedding choices, so it is
better decided now.

**10. Who reviews the reviewers?**
I have modelled `SECURITY_OFFICER` as separate from analysts. In a small
team that may be one person wearing both hats, which defeats the
separation. If so, decide now whether to enforce it or accept the risk
explicitly rather than discovering the gap in an audit.

## Open questions for the owner (2026-09-25)

Raised with the features added since Alpha 6. Neither is built either
way; each needs the owner's decision before anything changes.

**11. May an observation date be filled on Triage claims accepted before
Alpha 6?**
Claims accepted from Triage before Alpha 6 carry no `observed_at`. They
are listed by `scripts/legacy_records.py` and counted by the readiness row
`triage_claims_dated` (decision 80), and nothing fills them, because
writing a date onto a recorded claim rewrites it, which invariant 5
forbids. Filling them from the proposal's source would need an amendment
to invariant 5 that says when a claim may gain a field it never had, and
who may make that change.

**12. Does the second person on a case's merge switch need a seasoning
rule?**
Turning a case's merge switch off takes a second holder of `case.update`
on the case (decision 94), the same rule as every case two-person control
since 0028. So an account holding both SYS_ADMIN and CASE_OWNER can create
a second Lead investigator, assign it to the case and approve its own
relax at once. The seven-day countersigner rule (decision 92) already
exists as `iam.countersign_blocked_by`; applying it at decide for
`case.policy.relax`, and for node merges too, would refuse an approver
whose account someone else created, granted `case.update` or reset in the
last seven days, and an approver who did that to the requester. It
changes reviewed behaviour for case approvals, so it was not done unasked.
