# Alembic configuration. The database URL is NOT here — it comes from the
# DATABASE_URL environment variable (see db/migrations/env.py). Convention:
# secrets live in the environment or Vault, never defaulted in code.
[alembic]
script_location = db/migrations
file_template = %%(rev)s_%%(slug)s
prepend_sys_path = .
